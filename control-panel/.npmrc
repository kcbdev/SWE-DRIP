# better-auth declares an optional @sveltejs/kit peer whose Vite 8 conflicts with
# vitest's Vite 7. The Svelte peer is irrelevant to this Next.js app, so peer
# resolution is relaxed. See PBI-004 resolution / ADR-003.
legacy-peer-deps=true
