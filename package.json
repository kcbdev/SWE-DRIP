{
  "name": "swe-drip",
  "version": "1.0.0",
  "private": true,
  "description": "SWE Drip v1 — autonomous POD design pipeline + Control Panel (clean-room custom build)",
  "type": "commonjs",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "lint": "node scripts/lint.js",
    "test": "node --test tests/*.test.js",
    "verify:web": "cd control-panel && npm run build && npm run test",
    "verify:api": "python -m pytest api/tests pipeline/tests -q",
    "verify": "npm run build && npm run lint && npm test && npm run verify:web && npm run verify:api"
  }
}
