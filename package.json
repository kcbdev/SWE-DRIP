{
  "name": "swe-drip",
  "version": "0.1.0",
  "description": "SWE Drip — autonomous AI-run POD clothing brand for software engineers",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('build: docs-only repo, no compilation required')\"",
    "lint": "node scripts/lint.js",
    "test": "node --test tests/*.test.js",
    "verify": "npm run build && npm run lint && npm test",
    "verify:deploy": "node scripts/verify-deploy.js",
    "docker:build": "docker build -t swedrip:local .",
    "docker:run": "docker run --rm -d -p 8080:80 --name swedrip-smoke swedrip:local",
    "docker:smoke": "node scripts/docker-smoke.js"
  },
  "engines": {
    "node": ">=20"
  }
}
