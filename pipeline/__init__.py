"""SWE Drip LangGraph pipeline package.

Placeholder package for PBI-002. The 11-node ``StateGraph`` assembly, HITL
``interrupt()`` capability, and checkpointer wiring land in later PBIs
(PBI-010 onward). This package is imported by the API — the pipeline is a
library, not a separate deployable (ADR-002).
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
