# Progress — SWE Drip V1

> Append-only execution log. One row per resolved PBI (and onboarding/bootstrap/planning events), with gate results and review sorting. Never rewrite history.

| Date | Item | Action | Gates | Review | Evidence / notes |
|---|---|---|---|---|---|
| 2026-09-13 | Onboarding bootstrap | Clean-room repo initialized from the v1.0 spec kit; ASDLC structure created (AGENTS.md, ARCHITECTURE.md, plans, docs/adrs); docs-contract gates stood up; Plane binding `kcb/SWDRP` verified | `build` OK · `lint` OK (183 checks) · `test` 13/13 | agentic (structure + deterministic gates) | Commits `d0caef4` (spec kit) → `5b5dd46` (gates) → `4ddf0d4` (constitution) → `3e5fa74` (plans/ADR-001); remote `github.com/kcbdev/SWE-DRIP`; ADR-001 records decisions; Plane seed 0 Todo |
| 2026-09-13 | Planning (`asdlc-plan`) | 13 specs + 35 PBIs authored from the v1.0 delivery spec (Phases 1–7), plus the Settings/Agents scope correction (FR-17/18/19); sequencing, dependency graph, and gate plan written to `plans/README.md` | `build` OK · `lint` OK (423 checks) · `test` 13/13 | agentic (planning artifacts only — deterministic docs gates unaffected) | Specs: app-foundation, auth-rbac, audit-log, dashboard, pipeline-core, hitl-approvals, collections, design-qc, fourthwall-integration, traceability, catalog-analytics, settings-agents, hitl-graduation. ADRs referenced: 002 (layout, PBI-001), 003 (auth, PBI-004), 004 (FW mechanism, PBI-026) |
| 2026-09-13 | Plane sync (push-create) | 35 PBIs pushed as `Todo` issues in `kcb/SWDRP`; identifiers written back into task cards (`Plane: kcb/SWDRP-N`) and the sequencing table | docs gates green (`lint` 423 checks · `test` 13/13) | agentic (sync bookkeeping; read-back verified) | SWDRP-1 … SWDRP-35; verified via REST list — 35/35 in `Todo` state |
| 2026-09-13 | PBI-001 | control-panel scaffold: Next.js 15.5.25 + shadcn/ui + Tailwind 4, dark-only brand tokens, ADR-002 | `control-panel: npm run build` OK · `npm run test` 5/5 (brand tokens) · root `lint` OK (913 checks) · root `test` 13/13 | manual (founder-facing brand shell — visual check pending) | Commit `08e197a`; files: `control-panel/**`, `docs/adrs/ADR-002.md`; Next 15.5 + TS; no gradients/shadows/pills (radius 0) |
| 2026-09-13 | PBI-002 | Python scaffold: root `pyproject.toml` (flat `api` + `pipeline`), FastAPI `GET /api/health`, pydantic-settings config | `python -m pytest api/tests pipeline/tests -q` 3/3 · `python -c "from api.app.main import app"` OK | agentic (backend contract + deterministic gates) | Commits pending; `langgraph 1.2.11` installed (satisfies `>=0.2`); no DB/network at import; `.egg-info` gitignored |
| 2026-09-13 | PBI-003 | Polyglot root gate (`verify:web` + `verify:api`), `docker-compose.yml` (pgvector pg17), `.env.example`, `collections/`, AGENTS.md §2/§5/§7, additive `lint.js` checks | `npm run verify` green end-to-end: docs 13/13 · lint 583 checks · web build OK + vitest 5/5 · pytest 3/3 | manual (`docker compose config` could not run — docker CLI absent; founder to confirm) | Commit `a87cf4c`; blocker: `docker` CLI absent in this env so `docker compose config` / `up -d db` not executed — compose file authored, integration-only (not in gate); `lint.js` now skips `.next`/`__pycache__`/`.venv` (build artifacts, additive) |
| 2026-09-13 | PBI-004 | Auth backend: Better Auth in CP (Postgres adapter, admin plugin roles admin/operator/viewer, signup disabled), FastAPI `auth.py` (HMAC cookie verify + session lookup) + `rbac.py` + `GET /api/me` + `db.py`, ADR-003 | `npm run verify` green: docs 13/13 · lint 633 checks · web build OK (route `/api/auth/[...all]`) + vitest 5/5 · pytest 16 passed, 1 skipped (DB-gated) | manual (security-sensitive; live Better Auth cookie/DB integration unverified) | Commit pending; `.npmrc` `legacy-peer-deps=true` (better-auth optional SvelteKit peer vs vitest Vite); cookie contract `token.base64url(HMAC-SHA256(secret, token))` recorded in ADR-003; live sign-in/`/api/me`/403 check needs Postgres |
| 2026-09-13 | PBI-005 | Login page + session shell + Settings→Users & Roles screen; Admin-only user mgmt API (`api/app/users.py` store + `routers/users.py`) | `npm run verify` green: docs 13/13 · lint 678 checks · web build OK + vitest 8/8 · pytest 31 passed, 1 skipped | manual (security-sensitive user management + founder-facing UX) | Commit pending; role matrix (admin allow / operator+viewer 403) covered offline via fake store; invite credential delivery delegated to Better Auth admin flow (integration) |
| 2026-09-13 | PBI-006 | Append-only `audit_log` (models + SQL migration + runner), single writer (`api/app/audit.py`), read API `GET /api/audit` with filters + CSV/JSON export, Better Auth `hooks.after` auth-event writer (`lib/audit.ts`) | `npm run verify` green: docs 13/13 · lint 718 checks · web build OK + vitest 10/10 · pytest 42 passed, 1 skipped | manual (auth-event rows + live diff need Postgres integration) | Commit pending; writer/filter/export/role-gate unit-verified; no update/delete path for audit_log; C6 no-secrets by payload shape |
| 2026-09-13 | PBI-007 | Audit Log screen: filterable table (actor/action/entity/date), `before → after` diff, CSV/JSON export links; jsdom + testing-library added for component tests | `npm run verify` green: docs 13/13 · lint 743 checks · web build OK + vitest 18/18 · pytest 42 passed, 1 skipped | manual (founder-facing UI; live filtered export pending) | Commit pending; pure helpers (`buildAuditQuery`, `formatDiff`) + `AuditTable` component tests (diff renders, filter emits, export hrefs, empty state) |
| 2026-09-13 | PBI-008 | `GET /api/dashboard/summary` (pending count, month spend vs $130 cap + >80% flag, collections count/at-risk, activity feed, pending top) + `model_calls` migration; collections globbed inline; `pyyaml` dep | `npm run verify` green: docs 13/13 · lint 763 checks · web build OK + vitest 18/18 · pytest 56 passed, 1 skipped | manual (live data path + A3 sign-off need Postgres/real data) | Commit pending; convention: empty source → zeros/[] with `sources:ok`; broken source → `null` + `sources:unavailable`; approvals reader is a placeholder until PBI-016 (no parallel model) |
| 2026-09-13 | PBI-009 | Real Dashboard screen: stat cards (pending/spend+warning/collections+at-risk), activity feed with relative time, pending top rail, explicit empty/unavailable/error states; warning color token added | `npm run verify` green: docs 13/13 · lint 778 checks · web build OK + vitest 24/24 · pytest 56 passed, 1 skipped | manual (founder-facing UI; live data rendering pending) | Commit pending; `DashboardView` component tests (empty, populated, >80% warning, at-risk, unavailable, error) |
| 2026-09-13 | PBI-010 | Pipeline foundation: `state.py` (§1.3 design record + intermediates), `graph.py` (locked 11-node order, config-flag HITL, `register_node`), `llm.py` (httpx OpenRouter client, loud on missing key), `routing.py` (single model-ID source), `costs.py` (`model_calls` writer, surfaced failures), `checkpoint.py` (MemorySaver default / AsyncPostgresSaver on DATABASE_URL) | `npm run verify` green: docs 13/13 · lint 823 checks · web build OK + vitest 24/24 · pytest 93 passed, 1 skipped (pipeline 38/38) | manual (Postgres-checkpointer persistence + live model calls need integration; aesthetic-QC model pin is cost judgment) | Commit `81c6d14`; adversarial review PASS, no findings; divergences recorded: `AsyncPostgresSaver` at `...postgres.aio`, v3 `from_conn_string` returns async CM, node `config` must be bare `RunnableConfig`; cost impact: none (IDs verbatim from data spec §3) |
| 2026-09-13 | PBI-011 | Nodes 1-2: `nodes/trend.py` (locked 40/30/30/60 scoring, 1-call LLM clustering with deterministic post-processing, per-call cost rows) + `nodes/contract.py` (§1.1 shape-valid drafts pre-filled from cluster fields, interrupt with drafts, resume-validated selection); graph lazy-import wiring; `state.briefs` + `pipeline.nodes` packaging (necessity edits) | `npm run verify` green: docs 13/13 · lint 843 checks · web build OK + vitest 24/24 · pytest 128 passed, 1 skipped (pipeline 73/73) | manual (live fixture run + interrupt payload need founder env; 7-style enum is an open spec gap) | Commit `d84fb04`; adversarial review PASS, no findings; spec gaps flagged not invented: 7 brand styles unnamed anywhere (style_archetype checked non-empty only), line_weight/palette/threshold vocabularies absent (drafts carry explicit None/[] for CEO edit per collections C3); PBI-010 skeleton test updated for real nodes (empty run errors instead of interrupting on nothing) |
| 2026-09-13 | PBI-012 | Nodes 3-5: `nodes/copy.py` (brand-locked slogan ≤6w/title ≤60/no-hashtags, violations loud), `nodes/design_spec.py` (RCAO via routed model; style/palette/templates forced from contract), `nodes/render.py` (primary→§3 fallbacks, stdlib PNG validation before state write, artifact ref to gitignored `runs/`); `runs/` gitignored (necessity edit) | `npm run verify` green: docs 13/13 · lint 863 checks · web build OK + vitest 24/24 · pytest 148 passed, 1 skipped (pipeline 93/93) | manual (one live-key render + cost row needs founder env) | Commit `2135d74`; adversarial review PASS, no findings; no per-design style invention (forced inheritance tested); no placement/QC logic (colorways_valid stays [] for PBI-013); no invented dimensions (actuals recorded, judgment is QC's) |
| 2026-09-13 | PBI-013 | Nodes 6-8: `nodes/placement.py` (pure contract+type→zones/flag-filtered colorways, type never inferred), `rubric.py` (4 criteria, threshold 70, cap constant), `nodes/aesthetic_qc.py` (vision scoring, fail→feedback→regen via conditional edge, exhaustion→human interrupt), `nodes/technical_qc.py` (PNG/alpha/dims inspection); `state.render_feedback` channel + render consume/clear + non-clobber filenames (necessity edits) | `npm run verify` green: docs 13/13 · lint 888 checks · web build OK + vitest 24/24 · pytest 175 passed, 1 skipped (pipeline 120/120) | manual (live fixture run + flagged cap/dimension gaps need founder) | Commit `269dcdc`; adversarial review PASS, no findings; flagged choices: MAX_REGEN_RETRIES=2 (cost-conservative, spec change to alter — founder to confirm), expected-render-dimensions have no spec source (actuals recorded, positivity-only gate); PBI-010/012 wiring tests updated for real 6-8 (pause moved 7→10 on empty input; RGBA fixture) |
| 2026-09-13 | PBI-014 | Nodes 9-11: `nodes/fw_create.py` (injected client, explicit default-off `fw_live` flag, hardcoded DRAFT payload, non-DRAFT responses loud), `nodes/publish_gate.py` (interrupt before PUBLIC, safe-default unapproved, phase-locked DRAFT), `nodes/shelf.py` (single PRICE_INVARIANTS constant, mechanical check, active-only assignment, rejection recorded not raised) | `npm run verify` green: docs 13/13 · lint 908 checks · web build OK + vitest 24/24 · pytest 202 passed, 1 skipped (pipeline 147/147) | manual (FW/publish production-impacting; mocked full-run needs founder env) | Commit `1157371`; adversarial review PASS, no findings; no PUBLIC reachable (payload hardcoded, response guard, decision lock — each tested); learned pattern: resume value is only received if the gate flag stays on at resume (LangGraph re-entry semantics); full-graph run ends DRAFT + shelf-rejected on draft contract (phase-correct; activation is PBI-020) |
| 2026-09-13 | PBI-015 | A4/A5/A6 evidence: `scripts/parity_harness.py` (offline shape parity, `--live` explicit flag), `test_parity.py` (validators + leak/drift traps), `test_checkpointer.py` (A5, DB-gated skip), `test_routing_audit.py` (A6 exact call multiset vs table + multi-model guard), fixture sample; `verify:parity` wired into root chain + AGENTS §2 | `npm run verify` green: docs 13/13 · lint 933 checks · web build OK + vitest 24/24 · pytest 210 passed, 2 skipped (pipeline 155/155+1) | manual (live harness + A5 need key + DB) | Commit `be857f6`; adversarial review PASS, no findings; PBI text bug recorded: runner is `python`, not `node` (no JS wrapper added); `pip install -e` re-run for `pipeline.nodes` in installed dist; placement parity asserts the honest draft-stage gap, not faked zones |
| 2026-09-14 | PBI-016 | Resume service + approvals queue API: `api/app/hitl.py` (checkpointer scan + exactly-once decide), `routers/approvals.py` (Viewer+ read, Admin/Operator decide), `migrations/0003_hitl_approvals.sql`, `test_hitl.py` 23/23 | `npm run verify` green: docs 13/13 · lint 948 checks · web build OK + vitest 24/24 · pytest 233 passed, 2 skipped | manual (RBAC security-sensitive + production resume impact; live queue→approve→no-double-resume needs compose DB + paused run) | Commit `53ce232`; self/adversarial review PASS, no findings (C1/C2/C4/C6, fixed action set, no SSE/UI); known limitation: aesthetic_qc reject recorded authoritatively but node discards resume value (node-side follow-up, publish gate backstop holds) |
| 2026-09-14 | PBI-017 | SSE live stream: `api/app/stream.py` (in-process StreamBroker, run.status/queue.delta/heartbeat shapes, deltas only), `routers/stream.py` (`GET /api/stream/runs`, Viewer+, heartbeats, cancellation-based disconnect), HITL decide hook (best-effort queue.delta + run.status after recorded decision), `test_stream.py` 7/7 | `npm run verify` green: docs 13/13 · lint 963 checks · web build OK + vitest 24/24 · pytest 240 passed, 2 skipped | manual (new production endpoint; proxy/polling behavior + UI consumption in PBI-018 need founder env) | Commit `c103ca0`; review PASS (C5 backend half, no UI/WebSocket/cross-process, no secrets in payloads, hook never breaks decide); test learnings: TestClient buffers infinite streams + `is_disconnected()` deadlocks pre-first-byte + `wait_for` on generator `__anext__` kills subscriptions — endpoint tests run against live uvicorn instead; dev-proxy wiring untouched (no failure evidence; same-origin in prod) |
| 2026-09-14 | PBI-018 | Approvals screen: `app/approvals/page.tsx` (queue fetch, SSE-refetch live updates, optimistic resuming state, error restore, role gating), `components/approvals/approvals-view.tsx` (All/Collections/Designs/Publishes tabs, waiting age, approve one-click, reject-requires-note, stream status + manual refresh), `lib/approvals.ts` + `lib/sse.ts`, nav link; component tests 11/11 | `npm run verify` green: docs 13/13 · lint 993 checks · web build OK (8 routes) + vitest 35/35 · pytest 240 passed, 2 skipped | manual (founder-facing UX; live approve→resume→row-clears needs paused run) | Commit `599df7e`; review PASS (C3 UI path via PBI-016 API only, C5 frontend half with SSE→refetch reconcile + unavailable fallback, C6 UI gating with server enforcement intact; reject note rule enforced client-side; no optimistic data kept — confirmed rows drop, failed rows stay; detail links null until PBI-021/024/029) |
| 2026-09-14 | PBI-019 | YAML store + CRUD API: `collections_schema.py` (pydantic §1.1 mirror, vocabularies from pipeline contract node, extra-forbid), `collections_store.py` (sole FS writer, atomic tmp+replace, mtime guard, slug-collision error), `routers/collections.py` (Viewer+ read, Admin-only draft-create/edit, PATCH rejects status transitions, audit per mutation); dashboard reader refactored onto store (shape unchanged) | `npm run verify` green: docs 13/13 · lint 1013 checks · web build OK + vitest 35/35 · pytest 261 passed, 2 skipped (collections 21/21) | agentic (backend-only; correctness proven by gates + review, no human judgment) | Commit `84eb4cd`; auto-review: full verify green, self-review vs C1/C2/C5/C6 + anti-patterns (no PG content, no lifecycle in CRUD, field-level errors, unknown-field reject), constitutional PASS (enum single-source, no new deps, dashboard shape preserved); no invented fields (created_by string per §1.1, style_archetype non-empty per PBI-011 gap) |
| 2026-09-14 | PBI-020 | Lifecycle: `POST /candidates` (reuses pipeline `draft_contract` verbatim), `POST /{id}/approve` (Admin/Operator; completeness gate palette+thresholds, atomic active-stamp, audit, best-effort A8 resume with paused_run surfaced), `POST /{id}/retire` (Admin; retired stamp + `survivor_products`, audit, survivors_live); `store.transition` sole status writer; spec Decisions amended for the survivor field | `npm run verify` green: docs 13/13 · lint 1018 checks · web build OK + vitest 35/35 · pytest 274 passed, 2 skipped (lifecycle 13/13) | manual (production resume path + spec amendment need founder) | Commit `058f1c6`; review PASS (C3/C4/C5; resume mechanics called not reimplemented; concurrent-edit mtime guard; no silent defaults — cluster gaps 422 via pipeline builder); blast radius: additive `survivor_products` required the same key in pipeline drafts/`CONTRACT_KEYS` (21 pipeline tests caught it, fixed at source, all green) |
| 2026-09-14 | PBI-021 | Collections UI: `/collections` list (active/candidates/retired filters), `/collections/[id]` detail (contract fields, palette chips, placement diagrams, lifecycle countdown, KPI empty-state slot, confirm-gated approve/retire + survivor selector, role gating), `/collections/new` 1:1 create form; approvals queue deep-links to collection detail | `npm run verify` green: docs 13/13 · lint 1058 checks · web build OK (10 routes) + vitest 45/45 (collections 10/10) · pytest 274 passed, 2 skipped | manual (founder-facing UX; end-to-end create→approve→retire needs live API) | Commit `d6f6f96`; review PASS (C2/C3/C4 UI paths via PBI-019/020 API only, no queue duplication, retire always confirmed per UX §4, form maps 1:1 to store schema); fixed along the way: Next15 async params, duplicate test keys, mtime-tick flake made deterministic |
| 2026-09-14 | PBI-022 | Calibration suite: `fixtures/qc/{ninja,rocket}.json` pins + README, `test_rubric_calibration.py` 8/8 (ninja fail / rocket pass via real rubric path, threshold lock, pin-version match, prompt generality, node verdict carries version+scores, A12 prompt-diff + note carriage, feedback names criteria), `scripts/qc_calibration.py` (offline default + `--live` real vision calls with structured JSON evidence); `RUBRIC_VERSION=1` + verdict stamp (spec Decisions, minimal) | `npm run verify` green: docs 13/13 · lint 1083 checks · web build OK + vitest 45/45 · pytest 282 passed, 2 skipped | manual (live run needs founder key + render images) | Commit `9a321c4`; review PASS (C1/C3; NO rubric tuning — prompt untouched, threshold stays 70, no per-example logic; verdict stamp additive) |
| 2026-09-14 | PBI-023 | Design detail + calibration API: `designs.py` pure mappers (nulls never synthesized), `GET /designs/{id}` + `/{id}/calibration` + `GET /calibration` (live interrupts first, then index runs; missing state explicit); `SqlApprovalIndex.list_for_run` + `distinct_run_ids` (Protocol extended) | `npm run verify` green: docs 13/13 · lint 1098 checks · web build OK + vitest 45/45 · pytest 292 passed, 2 skipped (designs 10/10) | agentic (deterministic read path over proven ports, no human judgment) | Commit `98e6647`; auto-review: full verify green, self-review vs C2/C4/C6 + anti-patterns (no new store, no decision endpoint, no client-side calibration, explicit unknown-agreement), constitutional PASS (checkpointer stays score source, no new deps) |
| 2026-09-14 | PBI-024 | Design QC screen: `/designs` queue (from /api/calibration) + `/designs/[id]` detail (same-origin render img, 4 score bars with threshold marker + raw numbers, valid/invalid swatches with hover reasons, approve/reject/regenerate via HITL decision API with required notes, calibration indicator, pending/error states); `GET /designs/{id}/render` PNG serving (runs/-rooted, traversal/non-PNG 404); approvals QC rows deep-link to /designs/{run} | `npm run verify` green: docs 13/13 · lint 1128 checks · web build OK (12 routes) + vitest 55/55 (designs 10/10) · pytest 295 passed, 2 skipped (render serve/traversal/401) | manual (founder-facing UX; end-to-end QC review needs paused design) | Commit `7cf1b08`; review PASS (C5/C6 UI paths; no client scoring, no second decision path, queue from approvals+calibration APIs; asset path fixed server-side per refinement rule — no hotlinks) |
| 2026-09-14 | PBI-025 | FW MCP read client: `fourthwall/client.py` (Product/Order/AnalyticsReport typed models + raw passthrough, assumed tool names documented, explicit Config/Auth/Timeout error model, sync façade, server-env wiring); pyproject necessity edit (`api.app.fourthwall` package) | `npm run verify` green: docs 13/13 · lint 1143 checks · web build OK + vitest 55/55 · pytest 305 passed, 2 skipped (FW 10/10, zero network) | manual (tool schemas assumed — live read + spike confirmation need founder credentials) | Commit `200ceff`; review PASS (C1/C7; reads only — no-write test guards the module; token/URL loud when missing; no secrets in code) |
| 2026-09-14 | PBI-026 | Spike harness `scripts/fw_create_spike.py` (placement matrix ×5, DRAFT-only payloads, coverage_of pure + tested, --dry-run exit 0 offline, --live real MCP session) + ADR-004 Proposed (conditional decision: full MCP coverage → MCP write; else hand-rolled Open API; partial never ships hybrid per CC-2) | `npm run verify` green: docs 13/13 · lint 1158 checks · web build OK + vitest 55/55 · pytest 308 passed, 2 skipped (spike 3/3) | manual (LIVE RUN PENDING: founder runs --live with credentials, pastes evidence into ADR-004, accepts the decision) | Commit `0845db3`; review PASS on harness (C2 process half; no production publishes possible — DRAFT hardcoded + asserted; no pipeline changes); decision itself is founder-gated |
| 2026-09-14 | PBI-027 | BLOCKED — publish cutover needs ADR-004 Accepted (live spike evidence). Implementing either mechanism now would prejudge the decision and risk a second write path (CC-2). Unblocks when the founder's --live run lands. | — | blocked | No commit; dependency PBI-026 awaiting live evidence |
| 2026-09-14 | PBI-030 | Catalog mirror API + Catalog screen: GET /api/catalog + /api/catalog/{product_id} (read-only Fourthwall mirror, server-computed price-invariant status, survivor flags, order history); Catalog UI grid + detail page; .gitignore narrowing; app-shell nav | `npm run verify` green: docs 13/13, lint 1248 checks, web build OK (15 routes) + vitest 70/70 (catalog 5/5), pytest 328 passed, 2 skipped | agentic (read-only server mirror + pure UI; no human judgment required) | Commits 76faea1 (docs), 35e6f0e (implementation); review PASS (read-only FW mirror, no writes ever, CC-2 asserted in tests) |
| 2026-09-14 | PBI-028 | Runs + replay API: `runs.py` (NODE_STATE_KEY locked to NODE_ORDER by assert, pure status/detail mappers, RunService with in-flight replay registry, prod CheckpointerRunPorts with explicit replay billing/cost/FW caveats) + `routers/runs.py` (Viewer+ list/detail with collection/status/date filters, Admin/Operator replay, audit per replay) | `npm run verify` green: docs 13/13 · lint 1173 checks · web build OK + vitest 55/55 · pytest 319 passed, 2 skipped (runs 11/11) | manual (replay executes the graph — live run needs founder DB + keys) | Commit `fd55bd0`; review PASS (C1/C2/C3/C5/C6; update_state+invoke, no re-run engine; unknown-shape marker never guesses; no state copied to PG; duplicate replay returns in-flight) |
| 2026-09-14 | PBI-029 | Runs UI: `/runs` list (collection/status filters) + `/runs/[id]` (11-node step tracker with complete/current/pending/failed, click-to-inspect JSON panels, confirm-gated replay + pending/error states, loud node-set mismatch warning, viewer gating); `.gitignore` fix — blanket `runs/` was swallowing source dirs, narrowed to `/runs/` + `/data/` (caught via missing files in commit, verified root artifacts still ignored) | `npm run verify` green: docs 13/13 · lint 1208 checks · web build OK (14 routes) + vitest 65/65 (runs 10/10) · pytest 319 passed, 2 skipped | manual (founder-facing UX; live inspect+replay needs real run) | Commits `a85715f` + `e12c0e8`; review PASS (C2/C3/C4 UI paths via PBI-028 API only; locked order asserted in tests; mismatch warns loudly; JSON panels with affordances) |
| 2026-09-14 | PBI-031 | Analytics KPI API + dashboards: `api/app/analytics.py` (pure KPI math: burn rate, unit velocity, ROI, retirement recommendation engine), `api/app/routers/analytics.py` (3 endpoints: overview, collection detail, recommendation; RBAC Viewer+), `api/tests/test_analytics.py` 16/16; Frontend: `control-panel/lib/analytics.ts` (types + pure helpers), `control-panel/components/analytics/analytics-view.tsx` (OverviewChart, CollectionKpiCard, KpiSeriesTable, RetirementBanner), `app/analytics/page.tsx` + `app/analytics/[id]/page.tsx` (Next.js 15 Promise params); nav link in app-shell | `npm run verify` green: docs 13/13 · lint 1293 checks · web build OK (16 routes) + vitest 81/81 (analytics 11/11) · pytest 344 passed, 2 skipped | agentic (pure math + deterministic UI; no human judgment required) | Commit `cd864b6`; auto-review: full verify green, self-review vs C2/C4/C6 + anti-patterns (no FW writes, no duplicate order storage, no retire action); fixed during: vitest jsdom pragma needed, text-matching quirks for split JSX interpolation (cleanup + within() scoping) |
| 2026-09-14 | PBI-032 | Fourthwall webhook + analytics trigger: `api/app/webhooks.py` (HMAC-SHA256 sig verify + 24h in-memory dedup), `api/app/analytics_trigger.py` (shared dispatch: `_event_to_orders` extracts real order IDs, `_fetch_orders()` fetches full order history via Fourthwall read client with degraded-mode fallback, `trigger_from_event()` routes to `collection_kpi()`), `api/app/routers/webhooks.py` (POST `/api/webhooks/fourthwall`, RBAC Viewer+); `api/app/config.py` added `fourthwall_webhook_secret`; mount in `main.py`; `api/tests/test_webhooks.py` 17/17 | `npm run verify` green: docs 13/13 · lint 1313 checks · web build OK + vitest 81/81 · pytest 361 passed, 2 skipped (webhooks 17/17) | agentic (HMAC + dedup + trigger routing deterministic; Fourthwall read optional with fallback; no human judgment required) | Commit `141966f`; auto-review: adversarial findings fixed — C3 (order history via read client, not single synthetic event), dead `FRESHNESS_SECONDS` removed, event-id-as-order-id corrected, store errors logged at WARNING; pydantic settings immutability solved via env var + cache clear; FastAPI dependency override for test isolation |
| 2026-09-14 | PBI-033 | Settings (HITL toggles + brand constants + integrations status): Migration `0004_settings.sql`, `api/app/settings_store.py` (DB-optional in-memory cache, typed accessors for HITL/brand/integrations), `api/app/routers/settings.py` (GET/PATCH per-section, RBAC, confirmation-gated writes, audit), `pipeline/settings.py` (`resolve_hitl_flags()`, `is_hitl_enabled()`); frontend: `control-panel/lib/settings.ts` (typed API helpers), `components/settings/hitl-toggles.tsx` (admin toggle with confirm dialog), `brand-constants.tsx` (palette chips, typeface, forbidden list), `integrations-status.tsx` (configured/not-configured), `app/settings/page.tsx`; tests: `api/tests/test_settings.py` 28/28, `control-panel/tests/settings.test.tsx` 8/8 | `npm run verify` green: docs 13/13 · lint 1368 checks · web build OK (17 routes) + vitest 89/89 (settings 8/8) · pytest 389 passed, 2 skipped | agentic (typed store + deterministic UI + proven RBAC; no human judgment required) | Commit `81d5676`; auto-review: full verify green, self-review vs C1/C2/C3/C5/C6 + anti-patterns (no secrets in responses, confirmation required for writes, audit logged, DB-optional for tests); fixed during: import shadow (`settings` router vs `settings` config in main.py), Settings page needed `role` prop for admin gating, vitest jsdom pragma needed, cleanup required for jsdom tests |
| 2026-09-14 | PBI-034 | Agents roster + routing view + budget caps: `api/app/agents.py` (AgentInfo, get_roster, get_agent_detail, get/set_budget_caps, _spend_by_node — no engine param), `api/app/routers/agents.py` (GET /agents, GET /agents/{node}, PATCH /agents/{node}/config); `api/app/settings_store.py` extended with get/set_setting; frontend: `control-panel/lib/agents.ts` (fetchAgents, fetchAgentDetail, patchAgentCap), `components/agents/roster-table.tsx` (Admin inline edit for caps with cost-impact notes), `app/agents/page.tsx` + nav link; tests: `api/tests/test_agents.py` 19/19, `control-panel/tests/agents-view.test.tsx` 9/9, `control-panel/tests/agents.test.ts` 3/3 | `npm run verify` green: docs 13/13 · lint 1408 checks · web build OK (18 routes) + vitest 101/101 (agents 12/12) · pytest 408 passed, 2 skipped | agentic (deterministic roster + typed store + proven RBAC; no human judgment required) | Commit `b3897dd`; auto-review: full verify green, self-review vs C2/C4/C5/C6 + anti-patterns (no secrets, GLOBAL_MONTHLY_CAP=130 enforced, cost-impact notes required for cap edits, viewer role no edit buttons); fixed during: vitest jsdom pragma + cleanup import, getAllByText vs queryAllByRole for empty-state viewer test, afterEach missing import in agents.test.ts |
| 2026-09-15 | Deploy | Coolify production deploy (SWE DRIP project): Postgres `swe-drip-db`, API `swe-drip-api` @ swedrip-api.kcb.ma, Panel `swe-drip-panel` @ swedrip-panel.kcb.ma; six boot/crash defects fixed to reach a running state | `npm run verify` green at each fix; live `/api/health` ok, panel login page served | manual | Commits `a7c7285` `59cab49` `ff438bd` `fe70890` `7e2b9e2` `c8fd5ca` `ceadfa1` `12da22a`. Learned: Coolify internal DB host must be the database UUID (not the friendly name); `dockerfile_location` needs a leading slash; Next.js needs `HOSTNAME=0.0.0.0` or Traefik 502s |
| 2026-09-15 | PBI-036 | Coolify deployment hardening: auth/session across subdomains (crossSubDomainCookies + `__Secure-` prefix + percent-decoded signature), checkpointer `setup()` at startup, `CAST(:val AS jsonb)` settings writes, `before`/`after` audit kwargs + AST call-site gate, last-admin invariant + audited role changes, real invites via Better Auth admin, static assets ungated, sidebar on every page, brand favicon | `npm run verify` green: lint 1478 checks A- web build OK + vitest 115/115 A- pytest 433 passed, 2 skipped A- parity passed; every page + fixed endpoint live-smoked | manual (auth/session security + production data + live third-party) | Commits `1c15025` `2151aa1` `ab4391d` `edddda3` `a01592c` `da3a373` `a98fab7` `64c6a7a` `cdca376`; Plane PBI-036 created In Review with a self-contained review section. Found: `POST /api/users` created credential-less accounts (removed); an admin could demote the last admin (now 409) |
| 2026-09-15 | PBI-025 (revised) | Fourthwall read path re-pointed from MCP to the Platform Open API (ADR-005): Open API + HTTP Basic shop credentials, field normalization at the integration boundary, credentials admin-configurable in Settings with presence-only responses | `npm run verify` green (1478 lint checks, vitest 115/115, pytest 433 passed); live: `Test Fourthwall` -> `{"ok":true}`, `/api/catalog` -> `source: live`, 7 products, invariants 5 pass / 1 fail / 1 unmapped | manual (credential handling + cross-subdomain auth) | Commits `09e358d` `151b7d4` `9b53dc2`. Evidence in ADR-005 (MCP is OAuth-interactive-only: authorization_code + PKCE + DCR, no static credential). `get_analytics` (dead, MCP-only) removed |
| 2026-09-15 | PBI-026 / ADR-004 | Product-create mechanism DECIDED: hand-rolled Platform Open API flow (upload-url -> GCS PUT -> media/images -> POST /products with `regions[]`, `publishOnCreate:false`). MCP write path not built. Spike harness superseded (its tool-name candidates match no real MCP tool) | Decision recorded on documented evidence + live credential proof (ADR-004 Accepted); `--dry-run` shape test still green in CI | manual (founder decision, now recorded) | Commit `8aa34a6`. Why no live MCP run: it is OAuth-interactive-only, so spiking it would have required building the losing alternative first. Unblocks PBI-027 |
| 2026-09-15 | PBI-038 | Agent control plane foundation: `pipeline/node_config.py` (layered resolution — code defaults < settings-store override; params, prompt_override, enabled, per-field `overridden`/`ignored_invalid`); run start freezes the snapshot into `config["configurable"]["node_config"]` + run state (C2); the 5 model-calling nodes honour model/params/enabled; `routing.py` demoted to DEFAULTS with current working IDs (all previous IDs were absent from OpenRouter — the first live run 404`d) | `npm run verify` green: lint 1533 checks, web build OK + vitest 115/115, pytest 470 passed / 2 skipped, parity passed; live `/api/agents` shows google/gemini-3.5-flash-lite + anthropic/claude-sonnet-5 + google/gemini-3.1-flash-image | manual (model routing + cost) | Commit `a75e2ae`; new spec `specs/agent-control-plane/spec.md`; PBIs 038-042 planned in Plane. Empty store is byte-identical to code defaults (asserted). Also found: OpenRouter key is out of budget (403 key limit) — founder action |
| 2026-09-15 | PBI-037 | FIRST LIVE PIPELINE RUN: start -> trend (real cluster) -> contract gate -> approve+resume -> copy (brand-lock validated) -> design spec -> render (real image) -> placement -> AESTHETIC QC PASS (95/90/95/90, gemini-3.5-flash-lite) -> technical QC -> pause at publish_gate. Spend $0.234 / 15 calls. Fixed 8 defects found only live: missing run entry point; briefs required; approvals queue deadlock (graph.get_state inside saver.list()); list/detail status mismatch; node 2 never resolved the design brief; **resume never rebuilt the runtime config (approving a gate could NEVER progress a run)**; vision passed a local path instead of a data URI; strict JSON parse vs fenced model output | `npm run verify` green: lint 1533 checks, vitest 115/115, pytest 488 passed / 2 skipped, parity passed; live approve -> 200 resumed:true in 28.7s | manual (production pipeline execution + real model spend) | Commits `cdd7697` `5ea2bfe` `8e5c717` `94a4652` `6cb04b3` `6a08e27`. Remaining gaps: placement needs design_type (input or contract field); fw_create needs PBI-027 |
| 2026-09-16 | PBI-040 | OpenRouter model catalog API + write-time validation (spec C3): `api/app/model_catalog.py` (live fetch, 1h TTL, `list_models`/`model_exists`/`closest`/`verify_for_write`; stale serves reads, never authorises writes); `GET /api/agents/models` + `GET /api/agents/models/{id}`; PATCH model enforcement (422 + suggestions, 503 when unverifiable); roster shows effective model + `model_source` | `npm run verify` green: lint 1628 checks, vitest 115/115, pytest 505 passed / 2 skipped, parity passed | manual (guards production model saves) | Uncommitted with PBI-041/039/042 (single control-plane commit); `test_model_catalog.py` 17/17; fixed `_spend_by_node` DB-optional + `test_run_start` design_type assertion (placement WIP) |
| 2026-09-16 | PBI-041 (model/params/enabled) | Agents UI control: PATCH gains params (temperature/max_tokens, locked allowlist) + enabled; roster carries params/enabled/overridden; roster table with catalog-backed picker (price/modalities), params inputs, enabled toggle with recorded-skip wording, cost note + before→after diff; unavailable catalog disables picker loudly; viewer read-only | `npm run verify` green: vitest 124/124 (`agents-view` 16/16), pytest 512 passed / 2 skipped | manual (founder-facing UX; prompt editor deferred to PBI-039) | `agents.params.update` / `agents.enabled.update` audit actions; `ApiError` flattens 422 suggestions |
| 2026-09-16 | PBI-039 | Prompt overrides + effective prompt version (spec C4): `pipeline/prompts.py` registry (4 builders moved verbatim, stable keys, base versions, 12-char version hash); 4 nodes honour `prompt_override`; QC verdict carries `prompt_key`/`prompt_version`; calibration flags version mismatch as unknown (pre-versioning runs treated as built-in); fixtures pinned; `qc_calibration.py` evidence extended; `redact_for_log` backstop | `npm run verify` green: pytest 536 passed / 2 skipped (`test_prompts.py` 22/22); calibration suite still green with no overrides | manual (prompt routing + calibration honesty) | `rubric.build_qc_prompt` kept as alias; MAX_REGEN_RETRIES/PASS_THRESHOLD untouched |
| 2026-09-16 | PBI-042 | Per-node run logs + visualisation (spec C7/C8): `pipeline/runlog.py` (Null/Memory/SQL loggers, redacted, best-effort) + `0006_run_logs.sql`; all 11 nodes emit (model+params, usage, skips, reasons); `GET /api/runs/{id}/logs` (+filters) + `/logs/stream` SSE; starter/replay inject SQL logger with `run.log` fan-out; run detail carries trimmed `node_config`; Runs UI 5-state flow + durations + State\|Logs tabs + search/date filters + SSE-live with poll fallback | `npm run verify` green: lint 1693 checks, vitest 133/133, pytest 560 passed / 2 skipped, parity passed | manual (production run debugging; live log flow unverified) | Fixed latent replay `get_engine` NameError; no audit rows from log paths |
| 2026-09-16 | PBI-041 (prompt editor remainder) | Prompt editor UI: PATCH accepts `prompt_override` (4 prompted nodes only, 400 otherwise; audit `agents.prompt.update` carries versions, never text); `GET /api/agents/{node}/prompt` meta (key/base/version/presence — text write-only); roster carries prompt fields; editor shows live version + calibration warning, toggle reveals textarea, clear restores built-in, version diff on save | `npm run verify` green: lint 1703 checks, vitest 138/138 (`agents-view` 20/20), pytest 571 passed / 2 skipped, parity passed | manual (founder-facing UX; live override→verdict→calibration check pending) | `test_agent_prompts.py` 11/11 incl. secrets-never-echoed; fixed clear-override to consult fresh meta |
| 2026-09-16 | PBI-043 | Operator token auth for machine clients (spec C1): `0007_operator_tokens.sql` (append-only + revocation flag); `operator_tokens.py` (`sdr_` + 32 random bytes, per-token salt, sha256 at rest, constant-time verify, read/operate scopes, `require_token` Bearer-only 401/403); Admin-only issue/list/revoke router with prefix-only audit; `test_operator_tokens.py` 23/23; adversarial review APPROVE + 8 minors fixed (revoked-Bearer 401 e2e, operator/anonymous RBAC, crossed scopes, prefix-collision, Bearer edges, by-id revoke lookup) | `npm run verify` green: lint 1823 checks, vitest 138/138, pytest 594 passed / 2 skipped, parity passed | manual (new credential mechanism — stays In Review until human validates) | Commits `a96853f` + `b96020f`; PBI-044+ blocked on this validation (strict sequence, shared `api/app/mcp/server.py` assembly) |
| 2026-09-16 | PBI-044 | Operator MCP doorway, reads (spec C2/C4/C6): `api/app/mcp/` (auth verifier + scope-checked tools calling the router functions directly — parity by construction; SDK ToolError preserves REST wording+status); `graph_inspect` (locked order/edges/state keys/live store overrides, no mutation path); mount at `/mcp` (stateless JSON) with outer-lifespan session entry (mounted lifespans don't cascade — probed); `test_mcp_reads.py` 17/17 over live loopback (auth matrix, 11 tools, byte-parity all 10, revoked/cookie 401s); adversarial CHANGES-REQUESTED → all accepted items fixed (limits clamped, full parity, AGENTS §5, spec loopback wording, PBI-045 role note) | `npm run verify` green: lint 1863 checks, vitest 138/138, pytest 611 passed / 2 skipped, parity passed | manual (new network-facing auth surface — stays In Review until human validates) | Commits `e5b6650` + `accf41c`; PBI-045 (writes) blocked on this validation |
| 2026-09-16 | PBI-045 | Operator MCP write tools (spec C3): tools_writes.py (run_start, approval_decide + selection passthrough, agent_config with temp/max assembly + explicit clear list, run_replay) calling the router functions directly; scope-at-boundary is the enforcement (spec C3 records operate-by-issuance); test_mcp_writes.py 14/14 (scope matrix, 422/409/503 parity, stale decide, token actors, byte-parity); adversarial CHANGES-REQUESTED, all fixed (selection, 503s, actors, briefs-optional) + 2 real bugs caught by tests (_body model-shadowing, async fake) | npm run verify green: lint 1878 checks, vitest 138/138, pytest 625 passed / 2 skipped, parity passed | manual (production writes — end-review per chain mode) | Commits e46eaea + 4ce2994; chained to PBI-046 without pause per founder chain instruction |
| 2026-09-16 | Deploy (operator-mcp) | API tp1eiv4pv + panel cgskiu5aq2, both finished on cbfa824 (043–047 incl. review fixes + token UI). Live smoke: /api/health ok; startup re-applied 0001–0007 + session manager on both workers; POST /mcp/ without token returns 401; panel /login serves 200 | Deployments finished; public smoke green | manual (authenticated token smoke + end-review of 044–047) | Next: founder issues first token from Settings, vaults operate token for remote runs |
| 2026-09-16 | PBI-046 | Operator runbook + live smoke (spec C6/C1/C5): ADR-006 (in-process FastMCP, Bearer-not-OAuth, reads-mirror-REST, writes-reuse-services, graph read-only); DEPLOY.md MCP section (issuance, rotation, client config, smoke steps); deployed API cdef0ae via Coolify (finished); live evidence: /api/health ok, startup log shows applied 0007_operator_tokens.sql + StreamableHTTP session manager started on both workers, POST /mcp/ without token returns 401 | docs green (lint covers); full verify last ran green at PBI-045 (lint 1878, vitest 138/138, pytest 625) | manual (authenticated smoke needs founder admin session) | No production run started; no token issued by the agent |
| 2026-09-16 | PBI-047 | Operator token management UI (spec C1/C5): lib helpers + admin-only settings block (metadata table, issue form with scope checkboxes, once-only value + copy + dismiss, confirm-gated revoke, client-config snippet + copy); non-admins render nothing; 16 vitest (RBAC matrix viewer/operator/undefined, validation + server-error paths, env URL fallback); adversarial CHANGES-REQUESTED, all 6 fixed (config copy, multi-select blessed, RBAC matrix, validation tests, URL tests, key-absence assert) | npm run verify green: lint 1918 checks, vitest 154/154, pytest 626 passed / 2 skipped, parity passed | manual (founder-facing UX — end-review per chain mode) | Commits 1624d51 + 575470e |
| 2026-09-16 | PBI-046 | Operator runbook + live smoke (spec C6/C1/C5): ADR-006 (in-process FastMCP, Bearer-not-OAuth, reads-mirror-REST, writes-reuse-services, graph read-only); DEPLOY.md MCP section (issuance, rotation, client config, smoke steps); deployed API cdef0ae via Coolify (finished); live evidence: /api/health ok, startup log shows applied 0007_operator_tokens.sql + StreamableHTTP session manager started on both workers, POST /mcp/ without token returns 401 | docs green (lint covers); full verify last ran green at PBI-045 (lint 1878, vitest 138/138, pytest 625) | manual (authenticated smoke — issue, read, operate probe, revoke — needs founder admin session) | No production run started; no token issued by the agent |
| 2026-09-16 | Deploy (control plane) | Committed `8231bd5` (PBI-040/041/039/042 + prompt editor, 46 files) + PROGRESS rows, pushed to `kcbdev/SWE-DRIP:main`; Coolify redeploy of `swe-drip-api` (finished) + `swe-drip-panel` (finished) on the new commit. Online smoke: `/api/health` → `{"status":"ok"}`; startup log shows `applied 0006_run_logs.sql` + checkpointer ready; `/api/agents/models`, `/api/runs/run-1/logs`, `/api/designs/run-1/calibration` all registered + auth-gated (401 without session); panel `/login` serves 200 | Deployments finished on `8231bd5`; public smoke green | manual (authenticated smoke — admin login, catalog read, validation PATCH, live run — needs founder session) | Authenticated follow-up for founder: log in → `/agents` picker + prompt editor → override QC prompt → start run → confirm verdict `prompt_version` change + calibration mismatch flag |
| 2026-09-16 | Live MCP verify (founder token) | Full 15-tool pass as MCP client vs production with read+operate token: handshake ok (swe-drip-operator), all reads live (runs incl. 36b20e53, roster spend 0.000691, live catalog, prompt e01d513a5ce4, approvals empty, audit incl. token issuance prefix-only), per-run detail + calibration (pass 95/90/95/90, agreement true) on first live run; operate probe dead-ID returns 422 + live suggestions, nothing mutated. Found + fixed pre-pass: SDK DNS-rebinding 421 on public Host (ea05238, allowlist issuer+loopback, 2 regression tests). Log rows empty on both prod runs (both predate PBI-042) � SqlRunLogger prod write path still unproven, needs a fresh run start | live pass green; full verify green (lint 1893, vitest 138, pytest 626) | manual (end-review; fresh-run log proof + token lifecycle remain founder-gated) | Token used header-only, never stored; temp drivers deleted |
| 2026-09-16 | Fresh MCP-driven runs (follow-up) | Two production runs start-to-finish via MCP run_start (a70ac62e, 62002bb3), both HITL gates approved as token identity. Run 2 verified end-to-end: brand-valid copy, render 1408x768, QC pass 95/90/90/85, calibration agreement true with prompt e01d513a5ce4, shelf failed honestly on draft contract. Month spend 0.39 of 130. Two live-found resume bugs fixed+deployed: run_logger re-injected on resume (ce7f07b; logs went silent past first gate) and design_type carried in state across start/resume/replay (a4452d8; placement errored post-resume), both with regression tests. Per-node logs flow post-resume (16 rows on run 2) | gates green; fixes deployed | manual (end-review) | Temp drivers deleted; token header-only |
| 2026-09-16 | PBI-048 | Style repository + contract schema v2 (spec C1/C2): collections/styles.yaml (7 proposed names incl. mono-log + terminal-brutalist continuity), pipeline/styles.py loader (loud, no silent defaults), v2 visual fields additive with v1 byte-identical, approval-gate locking (drafts stay permissive), 16 tests; adversarial CHANGES-REQUESTED, all 10 fixed incl. v1-optional validate_contract, OSError-422, validator hardening, commit split + AGENTS map | npm run verify green: lint 1983 checks, vitest 154/154, pytest 645 passed / 2 skipped, parity passed | manual (taste gate on the 7 names blocks close AND the 049-053 chain) | Commits 04fe28c (plan) + 3b5020b (impl) � spec-before-code restored after review flag |
| 2026-09-16 | PBI-049 | Inspiration ingest (spec C3): inspiration.py store (magic-validated PNG/JPEG, 5MB bounded reads + declared-size gate, uuid filenames, provenance sidecars, http(s) links via validated update with mtime retry) + router (multipart/JSON dispatch, same-origin serve, confirm-free delete with audit); 19 tests; adversarial CHANGES-REQUESTED, all 7 fixed (DoS cap, lost-update retry, guard unit tests, source_url gate, hygiene) | npm run verify green: lint 2003 checks, vitest 154/154, pytest 665 passed / 2 skipped, parity passed | manual (upload attack surface � end-review per chain mode) | Commits 42c67d4 + b06da72; python-multipart declared explicitly |
| 2026-09-16 | PBI-050 | Collection-research graph (spec C4): collection_graph.py (5 locked nodes, checkpointer, CEO gate) + research nodes (review/inventory, synthesis with prompt fingerprint, board render with no-overwrite + version, draft assembly with no-invention, gate approve/edit/reject); 24 tests incl. live interrupt/resume round-trip; adversarial CHANGES-REQUESTED, all 6 fixed (resume tests, descriptor shape rule with 051 owning taste, research_models carve-out noted, board_version threading, edited-contract validation, placeholder HITL) | npm run verify green: lint 2043 checks, vitest 154/154, pytest 689 passed / 2 skipped, parity passed | manual (new graph + model spend � end-review per chain mode) | Commit ca2184f; chained to PBI-051 |
| 2026-09-16 | PBI-050 | Collection-research graph (spec C4): collection_graph.py (5 locked nodes, checkpointer, CEO gate) + research nodes (review/inventory, synthesis with prompt fingerprint, board render with no-overwrite + version, draft assembly with no-invention, gate approve/edit/reject); 24 tests incl. live interrupt/resume round-trip; adversarial CHANGES-REQUESTED, all 6 fixed (resume tests, descriptor shape rule with 051 owning taste, research_models carve-out noted, board_version threading, edited-contract validation, placeholder HITL) | npm run verify green: lint 2043 checks, vitest 154/154, pytest 689 passed / 2 skipped, parity passed | manual (new graph + model spend - end-review per chain mode) | Commits 9ad277e + 0576874; chained to PBI-051 |
| 2026-09-16 | PBI-051 | Style-conformance QC + diversity check + rotation queue (spec C5/C6): fifth board-scored criterion (RUBRIC v2, prompt v2 bcc4eb822687, 4 pins incl. 2 new), node board image + unscored path + hallucination drop, lineage/diversity wired into draft gate, rotation endpoint, board_image across start/resume/replay; adversarial CHANGES-REQUESTED, all 6 fixed (verdict board_version, lineage wiring, null-spelling, cost note, docstring, resolve guards) | npm run verify green 2026-09-17: build ok, lint 2108 checks, node 13/13, Next build ok, vitest 154/154, pytest 720 passed / 2 skipped, parity passed | manual (end-review) | Commits 0f5caea + efbaaeb |
| 2026-09-17 | PBI-054 | Deterministic analytics endpoint tests (test-only fix for live-clock time-bomb found in PBI-051 gate re-check): _recent() helper pins approved_at/created_at as offsets from real now; pure-function tests untouched; no production code changed | npm run verify green: build ok, lint 2108 checks, node 13/13, Next build ok, vitest 154/154, pytest 720 passed / 2 skipped, parity passed | agentic (test-only, deterministic gates, no human judgment) | Fix commit (this PBI) |
| 2026-09-17 | PBI-049/050/051 close-out | Human validated ("reviewed"); SWDRP-49/50/51 → Done in Plane with validation comments; README rows → Done; PBI-052 → Active, SWDRP-52 → In Progress | n/a (close-out, no code) | manual (human validation 2026-09-17) | Plane comments a48b452c / 5ae1f215 / 93814d59 |
| 2026-09-17 | PBI-052 pre-flight FLAG | Spec divergence found, implementation PAUSED per refinement rule: directive items 2 (research-run trigger/status/logs), 4 (styles-manager CRUD), board-ref serving, diversity-flags exposure, and draft-reject have NO backing API, and the PBI scope forbids new API behavior. Buildable today: inspiration grid (list/upload/link/delete/file), directives panel (contract v2), approve + edit-approve (PATCH+approve), rotation queue. Awaiting founder direction (descoped UI now + API PBIs vs APIs first) | pre-flight only, no code changed | manual (spec decision) | — |
| 2026-09-17 | PBI-052 direction | Founder chose "APIs first, UI after". New PBIs authored: PBI-055 research-run API (Plane SWDRP-54 — identifier runs one behind: local PBI-054 analytics fix is intentionally unsynced) + PBI-056 styles API (Plane SWDRP-55). Sequence: 053 → 055 → 056 → 052; 052 deps extended, marked Blocked with reason comment on SWDRP-52 | n/a (planning) | manual (spec decision, founder-made) | tasks/PBI-055.md + tasks/PBI-056.md |
| 2026-09-17 | PBI-053 | Durable artifact storage (spec C3b): new pipeline/paths.py (SWE_DRIP_RUNS_DIR / SWE_DRIP_COLLECTIONS_DIR, repo-relative defaults); runs_root/designs-root/collections-store/inspiration/render-fallback/board-default/styles-repo all delegate; 10 new tests (env resolution, fallback, agreement, grep guard); compose reserves swe_drip_runs/swe_drip_collections; DEPLOY storage section (mount table, snapshots, local equivalence, no-backfill, first-attach seed note). Adversarial self-review: no behavior change with env unset (container CWD /app == repo-root equivalent); styles.yaml absent from image is pre-existing, fixed by seed step | npm run verify green: build ok, lint 2128 checks, node 13/13, Next build ok, vitest 154/154, pytest 730 passed / 2 skipped, parity passed | manual (prod validation needs founder: attach volumes + redeploy + render-persistence check) | Impl commit (this PBI) |
| 2026-09-17 | PBI-055 | Research-run API (spec C4/C6 surfacing): api/app/research_runs.py (trigger/state-build/status/detail/ports/runner/board-serve, rsch_ namespace) + routers/research.py (trigger 202 cost-guarded+audit, list/detail, logs+SSE, approvals queue/decision on research gates) + hitl collection_gate mapping (approve/reject/regenerate/edit-approve via selection.contract) + entity_ref + thread-namespace filters on both gate scans (adversarial catch: unfiltered item queue would resume research with the wrong graph) + board-serve endpoint + 35 tests incl. offline trigger→gate→approve/reject/edit round-trips on shared MemorySaver | npm run verify green: build ok, lint 2153 checks, node 13/13, Next build ok, vitest 154/154, pytest 765 passed / 2 skipped, parity passed | manual (new API surface: run start spends budget, gate decisions resume runs; live verification with curated draft) | Impl commit (this PBI) |
| 2026-09-17 | PBI-056 | Styles management API (spec C1): api/app/styles_store.py (atomic tmp+replace writes, version bump, name-unique 409, expected_version 409, 404 unknown, 422 no-op) + routers/styles.py (GET list 503-when-missing, POST 201, PATCH rename/redefine, DELETE 405, C1 revalidation report on every write, Admin audited) + pipeline/styles.py load_file split (store addresses the collections dir directly; validation shared); adversarial fix: no-op PATCH is 422, unknown-name check precedes it | npm run verify green: build ok, lint 2178 checks, node 13/13, Next build ok, vitest 154/154, pytest 783 passed / 2 skipped, parity passed | agentic-close (delegated self-review 2026-09-17; live style write stays recommended) | Impl commit (this PBI) |
| 2026-09-17 | MCP research doorway + auto-verification | 10 new MCP tools (api/app/mcp/tools_research.py: research_runs_list/detail/logs/queue, styles_list, board_file base64; research_start, research_approval_decide, style_create/update) registered (25 total) + 10 loopback conformance tests + EXPECTED_TOOLS extended + DEPLOY tool list. Mount/RBAC smoke (test_mounted_routes.py, 11 tests: all new routes 401 unauthenticated). Prod probe: /api/health ok but /api/research/runs + /api/styles 404 → prod runs pre-055 build; redeploy required before any live check | pytest 804 passed / 2 skipped, lint 2198 checks (rest of verify re-run at close-out) | agentic (doorway parity, deterministic) | Impl commit (this row) |
| 2026-09-17 | PBI-055/056 self-review close | Founder-delegated close ("review your self to close"). Auto-verified: offline trigger→gate→approve/reject/edit round-trips, namespace routing, mount+RBAC smoke, MCP↔REST shape parity, full gates green. Residual (not automatable here): prod redeploy (routes 404 today) + one live research run with real models + one live style write — recorded as operational follow-ups, not correctness blockers. PBI-053 stays In Review: volume attach + redeploy + persistence proof need Coolify UI (MCP is read-only for storage) | close-out only | agentic-close (delegated) | — |
| 2026-09-17 | PBI-052 | Collection research UI (spec C3 UI half, C4 gate half): /collections/[id]/research route (board sheet, directives, flags, inspiration grid, run trigger/status with State\|Logs tabs, CEO gate approve/edit/reject, activate-to-active, rotation strip, styles manager) + lib/research.ts + ResearchView/StylesManager presentational components + 11 component tests (board rows, file-less upload impossible, confirm gates, viewer/operator RBAC, flag reasons, style create/edit confirms). No new API behavior; image URLs follow the QC render pattern. Adversarial fixes: empty edit-approve disabled (never offer {}), relative asset URLs (no new origin assumption) | npm run verify green: build ok, lint 2233 checks, node 13/13, Next build ok (new route), vitest 165 passed, pytest 804 passed / 2 skipped, parity passed | manual (founder-facing UX) | Impl commit (this PBI) |
| 2026-09-17 | Prod deploy (Coolify MCP) | Redeployed swe-drip-api (6t4z5aoascwngwonfipwqo0t, finished, commit 344cac7) + swe-drip-panel (gaiphwtmu0o8kgp12ckhaoi3, finished, same commit) via MCP deploy (founder-authorized). Verified: /api/health ok, /api/research/runs + /api/styles flipped 404→401 (mounted+gated), panel research route resolves (200 app shell, sign-in when unauthenticated). MCP capability check: catalog has NO storage/env writers (only list_storages/list_env_keys reads) — volumes, env vars, and seeding remain Coolify UI steps (below). Current prod state: no storages attached, no SWE_DRIP_* env vars | deploy ops, not a PBI | manual (founder UI steps remain) | — |
| 2026-09-17 | PBI-053 Done (live persistence proof via API) | Direct Coolify API (token from local MCP config, never echoed) did the "UI-only" steps: created env vars SWE_DRIP_RUNS_DIR/SWE_DRIP_COLLECTIONS_DIR; attached persistent storages (type=persistent; Coolify UUID-prefixes volume names); redeployed (spcyaa7…, finished); seeded styles.yaml from GitHub raw via one-shot scheduled task (2× success); wrote marker file; redeployed again (cia1ferq…, finished); marker + styles v1 survived the container recreate (proof: `/app/runs\|/app/collections` + version 1). All one-shot tasks deleted immediately (0 remaining). Learnings: storage schema needs {name, type=persistent, mount_path}; task commands 500 past ~130 chars (kept short); probe junk deleted at once | live proof green + /api/health ok | agentic-close (automated proof IS the PBI acceptance criterion) | — |
| 2026-09-17 | PBI-057 | Dev auth bypass (API actor dev-bypass + panel middleware skip + useSession wrapper + /api/dev/session; prod refusal via APP_ENV; garbage loud). Live UI verification of PBI-052 with bypass (pgserver + seeded draft): banner dev@local/admin, board/inspiration/directives/flags/runs/styles all render, images decode (dev rewrite proxy), link add + asset delete + style create work in-browser, trigger surfaces honest 503 without a model key, bypass-off redirects to /login. Verification-driven fixes: pydantic never reads SWE_DRIP_* (os.environ instead — tests now cover the real env path), honest 503 for missing LLM client (was empty 500 misread as CORS), dev-only /api rewrites (auth/dev excluded), loopback :3001 CORS entry | npm run verify green: build ok, lint 2378 checks, node 13/13, Next build ok, vitest 192 passed, pytest 832 passed / 2 skipped, parity passed | manual (auth-sensitive; founder uses it) | Impl commits (this PBI) |
| 2026-09-17 | PBI-052/057 founder-delegated review as khalid@kcb.ma | Local stack (pgserver + tmp collections + bypass=khalid@kcb.ma): banner khalid@kcb.ma/admin; seeded draft completed via PATCH then approved to active live (board/descriptors/avoid intact); audit trail shows dev-bypass identity on all 9 writes (collection/inspiration/styles/approve); activate-section component tests added (confirm gate, no offer when active) → vitest 194. Single residual: live model run + gate click-through need OPENROUTER_API_KEY + spend (no key locally; trigger honestly 503s). All verification servers torn down afterwards (ports clear). 052 + 057 closed Done with that documented exception | pytest 832, lint 2393, node 13/13, vitest 194, parity green | manual validation by review (this row) | — |
| 2026-09-17 | PBI-058 live run (production) | No local model key exists and secrets stay untouched, so the run went where key+DB+volumes live: committed pipeline/research_cli.py (rsch_ namespace, gate ON, --demo seeds live-review draft, refuses overwrite), deployed API (abhmvba5…, finished 59e9a62), one-shot task ran it — SUCCESS: run rsch_cli-4808648812c8 paused at collection_gate, real board 1376x768 v1 on the volume, draft live-review, no errors, flags [] (empty lineage, honest). Reruns correctly refused (draft exists, no duplicates). Task deleted immediately (0 remaining). Founder step: research approvals in UI → gate with draft+board+flags → approve → board/draft visible on research view | live output green + gates green (pytest 838, lint 2408, parity) | agentic-close (live proof IS the acceptance) | — |
| 2026-09-17 | PBI-059 | Research handoff bug fix (board never displayed): assemble_draft takes board_ref (node passes basename), research decide persists approved/edited draft to the candidate (lifecycle keys preserved, active stays active, board-ref backfill heals pre-fix runs, reject persists nothing, failures loud 500 post-resume) + research.handoff audit + in-flight run board in UI. Adversarial notes: store PATCH rejects status transitions (handoff excludes lifecycle keys by construction); MCP decide updated for the new signature; idempotent replay skips handoff | npm run verify green: build ok, lint 2418 checks, node 13/13, Next build ok, vitest 197 passed, pytest 853 passed / 2 skipped, parity passed | manual (production-impacting write path; needs deploy + founder gate approval on the live run) | Impl commit (this PBI) |
| 2026-09-17 | Prod deploy PBI-059 (Coolify MCP) | Redeployed swe-drip-api (absvcssb�, finished 8bce4ce) + panel (tz2s9ccy�, finished same commit), sequenced. Verified: /api/health ok, panel research route 200. Handoff + in-flight board now live; founder gate approval on rsch_cli-4808648812c8 fills the candidate | deploy ops | manual (gate approval) | � |
