# PBI-034: Wire 11 agents to Hermes gateway in Paperclip — CEO test cycle

## Directive

Rewire all 11 hired agents (CEO da47cca9, Trend Scout, Copy, Design, Listing, Social, Video, Analytics, Email, Community, Finance) in Paperclip `SWE Drip` (65913eaa-7de5-4372-8657-887328089e0a) to `adapterType: hermes_gateway` with `adapterConfig`: `apiBaseUrl: http://hermes:8642`, `apiKey` = Hermes `API_SERVER_KEY` stored as a company secret (secret_ref), `dangerouslyAllowInsecureRemoteHttp: true`, `sessionKeyStrategy: issue`, `persistSession: false` (issue #1160), `timeoutSec: 600`. Verify each agent's `testEnvironment` reports the gateway reachable. Fire one CEO decision-cycle task (reads program.md/program-summary) and confirm a **non-empty transcript with tool calls and recorded cost** posts back to the board. Keep per-agent models per `docs/03-agents.md`.

## Context

- Spec: `specs/agent-stack/spec.md` (contract 5) + `specs/paperclip-org/spec.md` (contract 3)
- Depends on: PBI-033
- Files/modules touched: `agents/hermes-gateway-config.json` (new — adapter config source of truth), `docs/agent-stack-wiring.md` (new — how-to + secret names), Paperclip runtime (agents + company secrets)
- Plane: `kcb/SWDR-34` (Todo — 9bc50567-5b27-468a-b374-f4665f56547d)

## Verification

- Links to agent-stack contract 5.
- `testEnvironment` → reachable for all 11 agents.
- CEO test task on the board shows a completed run with ≥1 tool call, a non-empty final message, and a cost line > $0.
- `agents/hermes-gateway-config.json` validates against the spec's adapterConfig schema.

## Refinement rule

- **Manual** PBI: live runtime verification against the deployed stack. If a run returns empty (no-op signature), check `persistSession` is false, confirm the insecure-HTTP toggle, and confirm `apiBaseUrl` resolves from the Paperclip backend — then retry once. If still empty, stop and report (suspected adapter/version mismatch).
