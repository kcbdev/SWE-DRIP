# PBI-033: Provision agent stack on Coolify — single Docker Compose service in paperclip project

## Directive

Provision the stack from `deploy/docker-compose.yml` as a **single Coolify Docker Compose resource** in project `paperclip` (dmaobray09x3xj6xe1kb2zke, env 7qbvoavtkwej8iwfmumrosru) on server `kcb`, pointing at `kcbdev/SWE-DRIP#master deploy/docker-compose.yml`. Set required env vars in the Coolify UI (never in the repo): `BETTER_AUTH_SECRET`, `PAPERCLIP_TOOL_ACTION_SIGNING_SECRET`, `POSTGRES_PASSWORD`, `OPENROUTER_API_KEY`/`OPENAI_API_KEY`+`OPENAI_BASE_URL`, `API_SERVER_KEY` (64-hex Hermes gateway key). Ensure `paperclip.kcb.ma` domain is bound to the paperclip service (port 3100, TLS). Verify per `specs/agent-stack/spec.md` contract 4. Do not delete old `paperclip` app (ddglphkkmg5apsosgh7q1crj) or old hermes service (kh85cuzhgfz1ib19x6d6es6i) — stop them only; removal is a founder decision after the new stack is verified.

## Context

- Spec: `specs/agent-stack/spec.md` (contract 4)
- Depends on: PBI-032
- Files/modules touched: `docs/agent-stack-provision.md` (runbook — created in PBI-032), Coolify UI on `coolify.kcb.ma`
- Plane: `kcb/SWDR-33` (Todo — 502ecaea-278e-46e3-96a0-f236b87a02b6)

## Verification

- Links to agent-stack contract 4.
- `curl -fsS https://paperclip.kcb.ma/api/health` → 200.
- `docker exec <hermes> hermes --version` → ≥ 0.20.5 (or hermes logs show gateway listening on 8642).
- `docker compose ps` (via server) shows postgres healthy, paperclip running, hermes running.
- Coolify MCP `search_resources { query: "paperclip" }` and `{ query: "hermes" }` show the new service `running:healthy`.

## Refinement rule

- This is a **manual** PBI: provisioning happens in the Coolify UI by the founder (or by me driving the UI under approval). If the REST/MCP create path is attempted and fails, do not loop — hand off the exact UI steps and wait for founder confirmation, then verify via MCP.
