# PBI-054: Deterministic analytics endpoint tests (live-clock time-bomb)

## Directive

`api/tests/test_analytics.py` endpoint tests pin fixed 2026-09 dates while
`api/app/routers/analytics.py:80` evaluates against real `now()` — once the
wall clock passed 2026-09-17 the 7-day eval window elapsed and
`test_recommendation_endpoint_no_rec` flipped red (found 2026-09-17 during
PBI-051 gate re-check; PBI-051 touches no analytics files). Make the four
live-clock endpoint tests date-relative (`approved_at`/`created_at` as
offsets from real now) so they hold on any date.

Scope boundaries: test file only — no production code, no threshold or
endpoint behavior change; pure-function tests (fixed `NOW`) stay untouched.

## Verification

- Executable: `python -m pytest api/tests pipeline/tests -q` green (720 passed / 2 skipped); `cmd /c "npm.cmd run lint"` green.
- Logic pins: window-not-elapsed (approved 1d ago) → `no_recommendation`; window-elapsed + units-below (approved 30d ago, 1 order) → `retirement_recommended`.

## Context

- Depends on: PBI-030 (analytics endpoints under test)
- Files: `api/tests/test_analytics.py`
- Plane: (unsynced — test-only fix, no founder judgment involved)
