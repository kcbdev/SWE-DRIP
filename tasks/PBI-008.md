# PBI-008: Copy Agent — slogan + listing_copy (FW dupe-proof)

## Directive
Implement the Copy Agent (`agents/copy/`) that turns an approved brief (CEO ≥60) into conversion copy without human touch. Read `workspace/design_briefs.json` → produce `workspace/listing_copy.json` per schema PBI-005: `{brief_id, primary_slogan ≤6w, slogan_variants[3], fw_title ≤60 keyword-rich, fw_description 150–200w SWE voice benefits-led, fw_tags[13], design_prompt_notes "JetBrains Mono Bold #00FF41 on #0D0D0D centered terminal", product_types [tshirt,hoodie,mug], product_url:null}`. Enforce before-write guards: `primary_slogan` word-count ≤6, dupe check exact phrase on Fourthwall + TeePublic (mocked `web_search`), never explain the joke inline, and "Would a senior SWE DM this?" voice gate (checked via literal anti-cringe blocklist + length). `fw_title` 13-tag `fw_description` invariants. Do not mutate Design/Listing — only the copy JSON.

## Context
- Spec: `specs/content-pipeline/spec.md` (contract 2 + Scope Copy Agent)
- Depends on: PBI-007 (needs approved brief) and PBI-005 (listing_copy schema)
- Files/modules touched:
  - `agents/copy/SOUL.md` (new — Sonnet, on_task, `file_read`/`file_write`/`web_search` tools, copy rules)
  - `agents/copy/agent.js` (new — `validateCopy()`, `buildFwTitle()`, `buildDescription()`, `dupeCheck()`, `writeListingCopy()`)
  - `tests/copy-agent.test.js` (new — ≤6w, >6w fail, dupe hit suppressed, tag/length invariants, phrase exact path)
- Plane: kcb/SWDR-9 (Todo — 4041275e-6700-4143-a8d5-3c7066776225)

## Verification
- Links to spec contract 2
- `npm run verify` green (fixtures, no live FW/TeePublic).
- Fixture brief `{phrase:"git push --force (and pray)", score:82}` → `primary_slogan` = same phrase (5 tokens inc parens) ≤6w passes, `fw_title` ≤60 and regex `programmer shirt|developer gift|rust|python` hits ≥2, `fw_description` token count in 150..200, `fw_tags.length===13` (approved), and `design_prompt_notes` includes `JetBrains Mono Bold` + `#00FF41` + `#0D0D0D`.
- Dupe path: `web_search` fixture returning exact hit for `"sudo make me a sandwich"` causes `dupeCheck` → skip and log `skip: dupe exact on FW`, `listing_copy.json` not appended with that brief (assert skip count).
- `listing_copy.json` validates against `schemas/listing_copy.schema.json` and `product_url===null` until PBI-009 fills it.
- `git crimes`/`incident culture` voices preserved dry (no motivational/hustle blocklisted phrases from docs/01-brand anti-patterns).

## Refinement rule
- If `fw_title` keyword density check vs docs/01-brand tiers diverges (new SEO terms added), update the cross-check list in `tests/copy-agent.test.js` from `docs/01-brand.md` tiers — do not weaken ≤6w guard to satisfy title length.
