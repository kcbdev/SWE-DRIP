# PBI-012: Nodes 3-5 — listing copy, design spec, art render

## Directive

Implement nodes 3–5: `pipeline/nodes/copy.py` (slogan ≤6 words, title ≤60, description, tags, no hashtags — brand voice), `pipeline/nodes/design_spec.py` (per-design brief resolved against the active collection contract: style/palette/placement inherited, never re-decided), `pipeline/nodes/render.py` (image generation via routing table — primary + fallback models; output normalized to PNG with expected dimensions; saves render artifact reference into state). Extend `pipeline/graph.py` wiring. Tests: copy rules enforced (word/length/no-hashtag); design spec inherits contract fields (assert no per-design style invention); render uses primary model then fallback on simulated failure; render output validated before state write.

Scope boundaries: no placement/colorway resolution or QC (PBI-013); no Fourthwall calls; no thumbnail/preview generation.

## Context

- Spec: `specs/pipeline-core/spec.md`
- Depends on: PBI-011
- Files/modules touched: `pipeline/nodes/copy.py`, `pipeline/nodes/design_spec.py`, `pipeline/nodes/render.py`, `pipeline/graph.py` (extend), `pipeline/tests/test_nodes_copy_spec_render.py`
- Plane: (pending sync)

## Verification

- Contracts C2 (nodes 3–5), C3, C4, C7 (design-spec shape) of the spec.
- Executable: `python -m pytest pipeline/tests -q` green (mocked image responses incl. fallback; spec inheritance assertions); `cmd /c "npm run verify"` green.
- Manual: with a live key, one real render lands in the (gitignored) run workspace; record cost row.

## Refinement rule

- If the image model's output format differs from expectations (e.g. no alpha channel), stop and record the finding; technical QC (PBI-013) owns format enforcement — do not patch around QC.
- Copy constraints are brand-locked; relaxations require founder approval via spec change.
