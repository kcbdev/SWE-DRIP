# PBI-016: CEO weekly loop + program.md drift compaction + scale unlock arithmetic

## Directive
Wire the CEO's Friday post-Analytics loop and the monthly program.md compaction that prevents drift: CEO reads `program-summary.md` (or `program.md` pre-90d) + `weekly_report.md` + `finance_report.md`, fires kill confirmations (already archived by Analytics), flags scale list for `Listing Agent` hoodie+mug expansion, triggers flash-sale `ecommerce_create-promotion` 20% off top 3 if WoW revenue −30% (from Analytics KPI), writes Telegram weekly digest (Markdown, KPIs + 3 CEO bullets + spend vs $130), escalates on `spend>$50 single decision | ban | payout delay | viral>10k`. Also implement monthly compaction job (1st, via Email Agent secondary task) `Summarize program.md → program-summary.md` — keep Winning patterns (deduped), merge duplicate patterns, archive >90d unreinforced to `## Archive`, keep Kill rules verbatim, trim Agent log to last 3 months, max 500 lines — after month 3 CEO reads `program-summary.md` first. Encode scale arithmetic `ordersNeeded(10000,38)=263/526 + visitorsNeeded(0.025)=21k, channel split SEO 28% Twitter 24% Email 14% Reddit 14% Collabs 10% TikTok 10%` + unlock table `$500 break-even → $1k newsletter → $2k collab → $3k stack-limited 48h → $5k paid ($300 Reddit+$200 Twitter, CAC<$12) → $8k influencer rev-share → $12k Discord #vote-next → $20k retail` as pure helpers. Do not mutate agent implementations.

## Context
- Spec: `specs/operations/spec.md` (contracts 5,6)
- Depends on: PBI-014 (weekly_report) + PBI-015 (finance_report + Telegram) + PBI-006 (program append/compact base)
- Files/modules touched:
  - `agents/ceo/agent.js` (new or extend `agents/ceo/SKILL.md` → `rules.js` with `postWeeklyDigest()`, `compactProgram()` caller, `scaleOrdersNeeded()`, `scaleUnlock()`)
  - `scripts/compact-program.js` (extend from PBI-006 if already stubbed — now wire CEO read order)
  - `tests/ceo-operations.test.js` + `tests/operations-drift.test.js` (new — digest markdown, drift 500-line guard, scale math)
  - `docs/adrs/ADR-003.md` (new — CEO loop + drift fix decision)
- Plane: kcb/SWDR-17 (Todo — 415eec93-5dd7-4642-87cf-54de9ca1dfea)

## Verification
- Links to spec contracts 5,6
- `npm run verify` green (FW promotion + Telegram mocked, no live `api.telegram.org`).
- Given `program.md` 600 lines with duplicate `Winning pattern:` + stale `>90d Rust Edition` + 4-month log → `compactProgram()` → `program-summary.md` ≤500 lines, duplicates merged (grep count 1), stale in `## Archive`, Kill rules verbatim, log 90d trimmed — asserted in `operations-drift.test.js`.
- `scaleOrdersNeeded(10000,38)` → 263 (or 526 for gross depending helper signature) + `visitorsNeeded(526,0.025)` → ~21040 (≈21k, ±5% tolerant) and `scaleChannels()` split sums 100% with labels SEO/Twitter/Email/Reddit/Collabs/TikTok — tested.
- `postWeeklyDigest(weeklyReport, financeReport)` capture `POST https://api.telegram.org/bot.../sendMessage` body `text` contains `SWE Drip — Week` + KPIs table excerpt + `OpenRouter budget: $X of $130` + `Action items`.
- CEO decision `flashSaleIfRevenueDrop` true only at ≤−0.30 else false — reused pure function from PBI-006 but triggered via `weekly_report` WoW field.
- Review comment records digest excerpt + `wc -l program-summary.md` + scale JSON.

## Refinement rule
- If `program.md` APPEND adds a 60-day repeat kill pattern, `compactProgram` still retains one copy as Winning — do not archive a still-in-kill-list pattern even at >90d (kill rules are hard constraints).
