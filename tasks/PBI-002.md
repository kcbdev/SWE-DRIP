# PBI-002: Provision Coolify application on server `kcb` (project swedrip)

## Directive
Create and configure the Coolify application `swedrip` on server `kcb` (project/s `swe-drip` — create project if absent) so it builds from `KCB/SWE-DRIP` `master` via Dockerfile (PBI-001's artifact), with env-var slots (not values), persistent-volume names reserved, health check `/health`, and auto-restart. Prefer Coolify UI flow from `docs/04-deploy.md:1.4–1.8`; document the API/MCP equivalent (`ajmcclary/coolify-manager` or `oakoss/agent-skills@coolify`) as a comment for future automation. Do not add the domain `swedrip.kcb.ma` yet (PBI-003) and do not mutate `Dockerfile`/`nginx.conf` (PBI-001).

## Context
- Spec: `specs/coolify-deploy/spec.md` (contracts 2, 5, 6)
- Depends on: PBI-001 (Dockerfile must exist or Coolify build fails)
- Files/modules touched:
  - `docs/adrs/ADR-002.md` (new — records image choice nginx:alpine, app type Dockerfile vs Docker Image, volume name reservations, env-var catalog)
  - `.coolify/app.json` or `coolify/app.json` or `docs/coolify-provision.md` (new — declarative capture of Coolify app config: server=kcb, project=swe-drip, app=swedrip, build_pack=dockerfile, port=80, healthcheck path=/health interval 30s, volumes future `paperclip-data:/app/data` + `paperclip-workspace:/workspace` as placeholders if decided, env-var names per `docs/02-stack.md` marked Secret)
  - `README.md` or `docs/04-deploy.md` (edit — add "kcb provision" delta as a 2-line note linking to ADR-002; do not rewrite the whole 7-day guide)
  - `scripts/coolify-provision.sh` (optional — idempotent `curl` against `https://<coolify-host>/api/v1/...` showing header `Authorization: Bearer $COOLIFY_API_KEY`, for review only)
- Plane: `kcb/SWDR-2` (Todo — 2bb2a422-1d20-465d-ac63-102f0a186bd1)
- Coolify surface: server `kcb` must be reachable; MCP server `coolify-manager` is optional aid, not a gate

## Verification
- Links to spec contracts 2, 5, 6
- `npm run verify` still green (no gate regression).
- Evidence in review comment:
  - Screenshot or `curl` against Coolify API showing app `swedrip` exists on server `kcb`, project `swe-drip`, build_pack `dockerfile`, git `KCB/SWE-DRIP#master`, port `80`, healthcheck `/health`.
  - Coolify UI → `swe-drip` → `swedrip` → Environment Variables lists expected names (`OPENROUTER_API_KEY`, `FOURTHWALL_MCP_TOKEN`, etc.) marked Secret where required, with placeholder or real-value masked — and `docker history` from PBI-001 still secret-free.
  - `docker logs` / Coolify → Deployments → first build log shows `Dockerfile` stages and `healthcheck` configured, status `Healthy` or `Running`.
- No domain attached yet — `https://swedrip.kcb.ma` is expected to be NXDOMAIN/unreachable at this PBI's review (assert that, don't fake it).
- If `kcb` Coolify is not yet installed, record `coolify install` blocker and keep PBI in `In Progress` with human gate note — do not fake a green deploy.

## Refinement rule
- If Coolify's API shape diverges from the canned `app.json`, update `docs/adrs/ADR-002.md` and the provision doc with the live response (redact secrets) — do not patch `Dockerfile` to compensate.
