# PBI-028: First product E2E — brief to live Fourthwall listing via hired agents

## Directive
Run the complete pipeline once through the hired Paperclip agents: CEO assigns an approved brief → Trend Scout output → Copy Agent slogan (≤6 words) + listing copy → Design Agent generates design and calls FW MCP `ecommerce_generate-product-design-previews` → `ecommerce_create-offers-from-designs` → Listing Agent sets price $32 / tags / Terminal Collection via `ecommerce_update-offer` and confirms live. Capture every task's output on the board. This is the proof the autonomous company functions.

## Context
- Spec: `specs/fourthwall-store/spec.md` (contract 4) + `specs/paperclip-org/spec.md` (contract 4)
- Depends on: PBI-024 (pipeline hired), PBI-026 (storefront ready)
- Files/modules touched: tracking only; `workspace/listing_copy.json` may receive the product_url record for repo-side memory
- Plane: `kcb/SWDR-28` (Todo — 588cf089-d951-484f-ad74-a4c7b24c6552)

## Verification
- Links to fourthwall-store contract 4
- Live product visible at `swedrip.fourthwall.com` — t-shirt, price exactly $32, in Terminal Collection, brand-correct preview image
- Task chain evidence: 4+ tasks on the board each with agent-attributed outputs (no human did the work)
- Founder visual approval of the first design before it stays published

## Refinement rule
- If any agent step fails (e.g. FLUX text rendering), follow that agent's soul fallback rules; if still failing after fallback, keep product unpublished and flag — never ship a broken first impression.
