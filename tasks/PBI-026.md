# PBI-026: Product-create spike + ADR-004 decision (A13)

## Directive

Deliver the A13 evidence: `scripts/fw_create_spike.py` — a manual/live spike that exercises the official Fourthwall MCP product-create tool against the placement matrix (front/back/sleeve, multi-region, colorways) and records parameter coverage: which placement fields survive, which are dropped or unsupported, with raw request/response shapes. Then write `docs/adrs/ADR-004.md` deciding the product-create mechanism for PBI-027 with that evidence: MCP write tool if it covers the matrix; otherwise the hand-rolled Open API (signed-URL upload) flow, with the decision's trade-offs. Update this spec's Decisions if the outcome refines C2's reading.

Scope boundaries: manual/live (credentials); no production publishes during the spike (DRAFT/sandbox targets only); no pipeline changes yet.

## Context

- Spec: `specs/fourthwall-integration/spec.md`
- Depends on: PBI-025
- Files/modules touched: `scripts/fw_create_spike.py`, `docs/adrs/ADR-004.md`, `specs/fourthwall-integration/spec.md` (Decisions update only)
- Plane: (pending sync)

## Verification

- Contract C2 of the spec.
- Executable: `python scripts/fw_create_spike.py --dry-run` (offline shape test, mocked response) exits 0; live run prints the coverage matrix + raw evidence.
- Manual: founder reviews ADR-004 evidence; decision recorded with the actual parameters observed.

## Refinement rule

- If the MCP write tool partially covers the matrix, ADR-004 must choose one mechanism for v1 — mixing both in production is a second write path (CC-2 violation) unless the ADR explicitly scopes a hybrid with a single entry point.
- Never spike against live published products; DRAFT only.
