# PBI-023: Connect Fourthwall MCP server in Paperclip + OAuth smoke test

## Directive
In Paperclip settings (org `SWE Drip`), add the Fourthwall MCP server: URL `https://mcp.fourthwall.com`, transport Streamable HTTP. Founder completes the one-time OAuth 2.0 browser login (Fourthwall account → select shop) — never paste credentials into chat. Then smoke-test from a Paperclip agent task: call `ecommerce_get-current-shop` and confirm it returns the SWE Drip shop record. Note: this corrects `docs/02-stack.md`'s static-token assumption (ADR-003 / fourthwall-store spec Decision-1).

## Context
- Spec: `specs/fourthwall-store/spec.md` (contract 1)
- Depends on: PBI-021 (org exists)
- Files/modules touched: tracking only; `docs/02-stack.md` may get a 1-line correction note pointing at this spec
- Plane: `kcb/SWDR-24` (Todo — 534e7b5c-2c13-447d-b377-8481042e6b5c)

## Verification
- Links to fourthwall-store contract 1
- Manual evidence: MCP server listed connected in Paperclip settings; smoke task output shows shop name/URL/currency JSON from `ecommerce_get-current-shop`
- Founder confirms shop selection was the SWE Drip shop

## Refinement rule
- If Paperclip's deployed version lacks MCP-server support, stop and flag with the exact UI gap — do not shim via external proxy without an ADR.
