# PBI-035: Full-power verification — persistent memory across restart + production model/max_tokens config

## Directive

Confirm the Hermes stack delivers "full power" persistence: (a) `docker exec <hermes> ls /opt/data` shows `sessions/`, `memories/`, `skills/`; (b) trigger a `control restart` of the hermes container and verify a second CEO cycle resumes prior session context and memory recall; (c) verify skills list still populated after restart. Apply production settings from `docs/03-agents.md` (swap test models gpt-4o-mini/gemini-flash-lite to roster models) and confirm `max_tokens: 4096` in `deploy/stack/hermes-config.yaml` is active in the runtime config (`hermes config get`). Enable `persistSession: true` only after confirming Paperclip issue #1160 (session-ID regex) is fixed on the deployed Paperclip version; otherwise keep `false` and record the limitation.

## Context

- Spec: `specs/agent-stack/spec.md` (contract 6) + `specs/llm-config/spec.md`
- Depends on: PBI-034
- Files/modules touched: `deploy/stack/hermes-config.yaml` (production models), `docs/agent-stack-wiring.md` (persistence notes), `plans/PROGRESS.md`
- Plane: `kcb/SWDR-35` (Todo — 7c509572-4498-452d-9dd2-48d42498cd94)

## Verification

- Links to agent-stack contract 6.
- `/opt/data` contents confirmed before and after restart; CEO cycle 2 references context from cycle 1 (or `hermes sessions` lists the resumed session).
- `hermes config get` shows `max_tokens: 4096` and the roster model for at least CEO.
- No 402 errors in hermes logs across two cycles.

## Refinement rule

- If memory does not persist across restart, the volume mount or session-key strategy is wrong — stop, re-check `/opt/data` mount + `sessionKeyStrategy`, update spec if the official layout differs. Do not ship "full power" claims without the restart-persistence proof.
