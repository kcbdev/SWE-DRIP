# PBI-011: Nodes 1-2 — trend research + clustering; collection contract approval

## Directive

Implement nodes 1–2: `pipeline/nodes/trend.py` (deterministic scoring ≥60 engagement/novelty/specificity + thematic clustering into collection candidates; LLM only for clustering synthesis where needed) and `pipeline/nodes/contract.py` (drafts the collection contract from an approved cluster — theme, style archetype, illustration rules, colorways with contrast flags, placement templates, KPI thresholds — and pauses on an `interrupt()` for CEO approval). Extend `pipeline/graph.py` with the node wiring. Tests: scoring rubric boundary cases; clustering groups briefs deterministically with mocked LLM; contract draft validates against the data spec §1.1 schema; interrupt state is recorded and resumable.

Scope boundaries: no copy/design/render work (PBI-012); no UI. Contract approval resumes only through the hitl-approvals mechanism (a stub resume in tests is fine).

## Context

- Spec: `specs/pipeline-core/spec.md`
- Depends on: PBI-010
- Files/modules touched: `pipeline/nodes/trend.py`, `pipeline/nodes/contract.py`, `pipeline/graph.py` (extend), `pipeline/tests/test_nodes_trend_contract.py`
- Plane: (pending sync)

## Verification

- Contracts C2 (nodes 1–2), C3, C4 of the spec.
- Executable: `python -m pytest pipeline/tests -q` green (score 60/61 boundaries; mocked LLM clustering; YAML contract schema fields; interrupt + resume path); `cmd /c "npm run verify"` green.
- Manual: run node 1 against fixture briefs → cluster candidate appears in state; node 2 pauses with an interrupt payload.

## Refinement rule

- Scoring weights are locked (engagement 0–40, novelty 0–30, specificity 0–30, pass ≥60) — changes require a spec amendment, not a code tweak.
- If clustering needs embeddings/vector search beyond scope, keep it LLM+deterministic and record the decision; do not introduce pgvector dependencies silently.
