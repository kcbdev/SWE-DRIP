# PBI-015: Parity harness + checkpointer verification + routing audit (A4/A5/A6)

## Directive

Deliver the Phase-2 acceptance evidence: `scripts/parity_harness.py` runs brief samples through the graph with mocked model and Fourthwall layers and asserts output shapes (clustered brief set → contract shape; brief → design-spec shape; render ref; QC scores; placement; price) — the A4 offline parity check that the publish step stays uncut. Add `pipeline/tests/test_checkpointer.py` (A5: with `DATABASE_URL`, a full run's persisted state matches the design record schema; skipped offline) and `pipeline/tests/test_routing_audit.py` (A6: mocked transport asserts every node's used model equals `pipeline/routing.py` for a full run). Fixtures for brief samples under `pipeline/tests/fixtures/`.

Scope boundaries: no live model or Fourthwall calls in the harness; parity is shape/consistency, not pixel or prose equality.

## Context

- Spec: `specs/pipeline-core/spec.md`
- Depends on: PBI-014 (all nodes), PBI-003 (gates), PBI-010
- Files/modules touched: `scripts/parity_harness.py`, `pipeline/tests/test_parity.py`, `pipeline/tests/test_checkpointer.py`, `pipeline/tests/test_routing_audit.py`, `pipeline/tests/fixtures/*`, root `package.json` (wire harness into verify only if offline)
- Plane: kcb/SWDRP-15

## Verification

- Contracts C5, C6, C7 of the spec.
- Executable: `python -m pytest pipeline/tests -q` green; `node scripts/parity_harness.py` exits 0 offline; `cmd /c "npm run verify"` green.
- Manual (with `DATABASE_URL` + `OPENROUTER_API_KEY`): run the harness live for one sample and record the cost rows.

## Refinement rule

- If output-shape parity exposes a mismatch, fix the pipeline implementation or amend the spec — never weaken the parity assertions to pass.
- The harness must stay offline-capable; live mode is an explicit flag.
