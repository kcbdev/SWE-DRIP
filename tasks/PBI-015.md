# PBI-015: Email + Finance agents — Loops & payout reconciliation + Telegram escalations

## Directive
Implement the Email Agent (`claude-sonnet-4-6`, Sat 09:00, budget $5, `POST https://app.loops.so/api/v1/campaigns` with `LOOPS_API_KEY`, FW `ecommerce_list-offers` new products) and Finance Agent (`gemini-2.0-flash-001`, 1st 09:00, budget $2, FW `ecommerce_get-payout-transactions` + `ecommerce_get-payout-info` → `/workspace/finance_report.md` `Revenue | COGS | FW fee 2.9%+0.30 | OpenRouter spend | Net%`, top revenue design, payout expected/received + MoM, cap $130 usage %, escalate to CEO if payout delayed >3d || OpenRouter >80% by 20th || net<40%) sharing the Telegram helper `notify_founder` (`POST https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage {chat_id:$TELEGRAM_CHAT_ID, text, parse_mode:"Markdown"}`) with CEO (from PBI-006). Archive finance `workspace/reports/archive/` weekly. Do not mutate Analytics.

## Context
- Spec: `specs/operations/spec.md` (contracts 2, 3, 4)
- Depends on: PBI-014 (needs weekly_report for Email context + offers for Finance)
- Files/modules touched:
  - `agents/email/SOUL.md` + `agents/finance/SOUL.md` (new)
  - `agents/email/agent.js` (new — `send_newsletter(subject, html, listId)`, `buildAgentReportWeekly()`)
  - `agents/finance/agent.js` (new — `buildFinanceReport(payoutFixture)`, `shouldEscalate()`, `archiveReport()`)
  - `workspace/finance_report.md` + `workspace/reports/archive/.gitkeep` (new)
  - `tests/email-finance.test.js` (new — Loops shape, Finance math, escalation guards, archiving)
- Plane: kcb/SWDR-16 (Todo — 94055e2e-6f84-4114-a015-4adf67849542)

## Verification
- Links to spec contracts 2,3,4
- `npm run verify` green (Loops/Telegram/FW mocked, no live calls).
- `send_newsletter("The Agent Report #1 — exit 0", "<html>…</html>", "list_123")` captures `POST https://app.loops.so/api/v1/campaigns` with `Authorization: Bearer $LOOPS_API_KEY` and body keys `subject/html/listId`; subject prefix `The Agent Report #` and sections `This week the Trend Scout found` + `The Copy Agent produced` + `What got killed` + `Next week's brief` snapshotted.
- `buildFinanceReport({revenue:1000, cogs:400, fee:29.3, openRouter:100})` → `Net: $470.7 (47%)` (assert math), `shouldEscalate({payoutDelayDays:4})===true`, `{spendPctBy20th:0.85, day:21}===true`, `{netMargin:0.35}===true`, otherwise false; MoM `+12%` string included.
- `archiveReport()` copies `finance_report.md` → `workspace/reports/archive/finance-YYYY-MM.md` (assert file exists via `git ls-files` not needed, just FS check in test).
- Telegram mock capture `POST .../sendMessage` with `parse_mode:Markdown` for escalation path.

## Refinement rule
- If Loops API migrates `html` → `bodyHtml`, shim in `send_newsletter` to send both keys — do not drop the subject prefix guard.
