# PBI-006: CEO decision rules + program.md lifecycle + brand invariant lint

## Directive
Make `program.md` APPENDABLE and `program-summary.md` COMPACTABLE and the CEO rules PURE with tests, and extend lint to guard brand constants. Create `agents/ceo/SKILL.md` skeleton (reads program-summary.md then weekly_report.md, enforces approve≥60/kill 30d/scale 5-per-14d/flash −30%), add `scripts/append-program.js` (dedupe 60-day window) + `scripts/compact-program.js` (keep Winning patterns + Kill rules, archive >90d unreinforced to `## Archive`, trim Agent log to 90d, max 500 lines, merges duplicates), and extend `scripts/lint.js` to fail if `docs/01-brand.md` is edited to change `#0D0D0D`/`#00FF41`/`#FF6B35`/`JetBrains Mono`/`$32`/`$62`/`$20` without `docs/adrs/ADR-*.md` containing "Pricing" or "Brand", and to fail if `AGENTS.md:56-130` still says `Planned` for a landed module (check `git ls-files agents/ceo workspace`). Do not touch Dockerfile/domain/pipeline agents.

## Context
- Spec: `specs/platform-workspace/spec.md` (contracts 2, 3, 4, 6)
- Depends on: PBI-005 (needs workspace dir + schemas for `program.md` append path)
- Files/modules touched:
  - `agents/ceo/SKILL.md` (new — CEO identity + decision rules + program-summary read order)
  - `program.md` (new — seed with `## Winning patterns` + `## Kill rules` + `## Archive`)
  - `scripts/append-program.js` (new — 60-day dedupe)
  - `scripts/compact-program.js` (new — 500-line trim)
  - `scripts/lint.js` (edit — add brand-invariant + Context Map checks; keep existing docs/01-brand + stack + agent roster checks)
  - `tests/platform.test.js` + `tests/program-drift.test.js` (new — pure CEO rules + append/compact)
  - `AGENTS.md` (edit only if Context Map needs to mark `workspace/` as `Live: workspace/` after PBI-005, else leave for review)
- Plane: kcb/SWDR-6 (Todo — 07e62377-242d-4cd0-a7bd-bcb21b8c01ac)

## Verification
- Links to spec §Contracts 2,3,4,6 + docs/07-scale drift fix
- `npm run verify` green (new tests pass, lint passes with new checks).
- `node -e "let {decideApproveBrief,decideKill,decideScale,decideFlashSale}=require('./agents/ceo/rules.js'); assert(decideApproveBrief(60)===true); ..."` 100% branches via `node:test` (or inline `agents/ceo/rules.js` CommonJS/ESM).
- `node scripts/append-program.js "Winning pattern: incident culture converts 3x" && node scripts/append-program.js "Winning pattern: incident culture converts 3x"` → second is no-op (dedupe), `grep -c "incident culture"` in `program.md` ==1.
- Fixture `program.md` 600 lines with duplicate Winning patterns + stale >90d + 4-month log → `node scripts/compact-program.js` → `program-summary.md` ≤500 lines, duplicates merged, stale under `## Archive`, Kill rules retained verbatim — asserted in `tests/program-drift.test.js`.
- Lint guard: temporarily edit `docs/01-brand.md` to replace `#0D0D0D` with `#111111`, `node scripts/lint.js` → exit 1 listing "brand invariant"; revert. AGENTS map check: `git ls-files agents/ceo/SKILL.md` exists but `AGENTS.md` still says `agents/: Planned: ceo/SKILL.md — see docs/03-agents.md` → lint fails (forces Context Map update in this PBI's commit).

## Refinement rule
- If CEO rules need async (e.g., `sumAgentSpend()` reads `workspace/reports`) make that helper async and test with file fixture — do not make `decide*` depend on OpenRouter spend API.
