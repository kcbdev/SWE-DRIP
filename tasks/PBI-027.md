# PBI-026: Storefront design live — Brutal theme, brand CSS, collections, shop online

## Directive
Implement the storefront design on Fourthwall (dashboard operations per fourthwall-store spec Decision-2): activate **Brutal theme**; inject Custom CSS exactly `:root { --color-background:#0D0D0D; --color-primary:#00FF41; --color-text:#FFFFFF; --font-display:'JetBrains Mono',monospace; }`; set mono wordmark logo (extract via MCP `brand_from_url` on `https://swedrip.kcb.ma` + `brand_extract_assets` if needed); create collections `Terminal Collection` + `Stack-Specific` via MCP `ecommerce_create-collection`; set shop site status **online** (`ecommerce_update-shop-site-status`). Verify public storefront renders branded.

## Context
- Spec: `specs/fourthwall-store/spec.md` (contracts 2, 3, 5)
- Depends on: PBI-023 (MCP connected for collection/site-status calls)
- Files/modules touched: tracking only
- Plane: `kcb/SWDR-27` (Todo — 94944011-98ac-4b05-949e-d101463e1891)

## Verification
- Links to fourthwall-store contracts 2, 3, 5
- `https://swedrip.fourthwall.com` → 200 publicly, page HTML/CSS contains `#0D0D0D` and JetBrains Mono reference (or founder visual confirm)
- `ecommerce_get-collections` returns both collections

## Refinement rule
- If Brutal theme is unavailable on the shop's plan, choose the closest dark minimal theme and flag for founder approval — never ship default light theme.
