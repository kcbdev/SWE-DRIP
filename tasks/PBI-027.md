# PBI-027: Publish cutover — decided mechanism live + old-path verification (A14/A15)

## Directive

Implement the ADR-004 decision behind pipeline-core's injected Fourthwall interface: `api/app/fourthwall/writes.py` (the chosen create mechanism, server-side secrets, price/collection from the shelf result — never literals); enable `pipeline/nodes/fw_create.py` live mode and the publish gate's `PUBLIC` transition (previously DRAFT-only); verify A14 end-to-end (one real product through the pipeline, Shelf check passing, correct in Fourthwall) and A15 (old Paperclip/Hermes publish path confirmed inactive, evidence recorded). Extend tests with mocked create responses for both success and rejected-price paths.

Scope boundaries: single write path only (no script reaches production data); no Control Panel write endpoints; no changes to the shelf pricing logic itself.

## Context

- Spec: `specs/fourthwall-integration/spec.md`
- Depends on: PBI-026 (decision), PBI-014 (fw_create/publish gate/shelf), PBI-016 (resume), PBI-025
- Files/modules touched: `api/app/fourthwall/writes.py`, `pipeline/nodes/fw_create.py` (extend), `pipeline/nodes/publish_gate.py` (enable mode), `pipeline/tests/test_nodes_fw_publish_shelf.py` (extend), `plans/PROGRESS.md` (evidence entry)
- Plane: (pending sync)

## Verification

- Contracts C3, C4, C5, C6 of the spec.
- Executable: `python -m pytest pipeline/tests -q` green (mocked create; price-rejection path never calls the writer); `cmd /c "npm run verify"` green.
- Manual (founder, live): A14 — publish one real product end-to-end and verify it in Fourthwall at the invariant price; A15 — confirm the old path is inactive; both evidenced in the resolution comment.

## Refinement rule

- If A14 fails mid-cutover, revert to DRAFT-only immediately and record the failure — never leave a half-published state or a second active path.
- Any deviation from ADR-004's mechanism requires the ADR to be updated first.
