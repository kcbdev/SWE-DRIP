# PBI-018: Launch readiness gate + PH/HN drafts + channel rule lint

## Directive
Gate launch on truth not vibe: add `scripts/verify-launch.js` that reads `workspace/reports/launch_readiness.json` (or counts `ecommerce_list-offers` mock with `status:active` + `workspace/reports/video_clips.json.length==3` + `Buffer queue length==20` + `Loops draft exists` + `email list >=50` stub + `ph_hunt_draft.md` + `show_hn_draft.md` existence + `@swedrip` bio/pinned + Reddit karma fixture) and prints `MISSING: ...` and exits 1 unless ALL `docs/05-launch.md` Pre-launch checklist items are true (10 validated live designs + 3 videos + 20 tweets pre-scheduled + PH tagline+description+screenshots + Show HN architecture draft + launch email held + Buffer connected+Loops test-sent+Telegram + Analytics test + Finance test + all 11 agents Running). Also create draft templates `ph_hunt_draft.md` (Name: SWE Drip / Tagline: A clothing brand run entirely by AI agents. No humans involved. + PH screenshots FW homepage/Paperclip board/Design render/Analytics report) and `show_hn_draft.md` (Show HN infrastructure angle), and extend lint to assert `social_state.json hashtags===0` and `reddit_posts[0].body.includes('swedrip.fourthwall.com')===false` per channel rules. Wire `verify-launch` into `npm run verify` as skipped-until-`launch_readiness.json` until pre-launch PBI lands (never red offline). Do not mutate storefront CSS or agent crons.

## Context
- Spec: `specs/storefront/spec.md` (contracts 4,5,6)
- Depends on: PBI-017 (storefront ready) + PBI-011/012 (Social/Video provide queue+clips) + PBI-014 (Analytics test) — sequenced after storefront for linear review
- Files/modules touched:
  - `scripts/verify-launch.js` (new — launch gate)
  - `ph_hunt_draft.md` + `show_hn_draft.md` (new — templates per docs/05-launch)
  - `scripts/lint.js` (edit — add `launchRulesCheck()` for hashtags 0 + Reddit body no link)
  - `package.json` (edit — add `verify:launch` script chaining `node scripts/verify-launch.js`; gated skip if `launch_readiness.json` absent)
  - `tests/launch.test.js` (new — verify-launch happy/fail, PH/HN template strings, channel rules)
- Plane: kcb/SWDR-19 (Todo — fb0175aa-3058-4cd9-94b5-974684dbbd99)

## Verification
- Links to spec contracts 4,5,6
- `npm run verify` green offline (verify-launch skipped when `launch_readiness.json` absent, never fails local gate).
- Fixture happy: `launch_readiness.json {offersActive:10, videoClips:3, bufferQueued:20, loopsDraft:true, emailList:55, phDraft:true, hnDraft:true, telegramOk:true, agentsRunning:11}` → `node scripts/verify-launch.js` exit 0; any field below threshold → exit 1 with `MISSING: offersActive 10` etc. asserted in `tests/launch.test.js`.
- Template strings: `readFileSync("ph_hunt_draft.md").includes("Name: SWE Drip") && includes("Tagline: A clothing brand run entirely by AI agents") && includes("Screenshots:")` true; `show_hn_draft.md` includes `Show HN:` + `Paperclip` + `OpenRouter` + `Fourthwall`.
- Channel lint: `social_state.json` fixture with `hashtags:1` → `launchRulesCheck()` exit 1; `reddit_posts[0].body = "https://swedrip.fourthwall.com/products/x"` → lint fail (must be comment-reply only).
- Targets doc `Weekly $300+ / PH Top 10 / HN >50` echoed in `weekly_report.md` target column (cross-check not hard fail).

## Refinement rule
- If launch week sequence shifts (PH Thu → Wed) update `ph_hunt_draft.md` date note but keep the 10-product/20-tweet/3-video gates — checklist gates are non-negotiable, schedule is negotiable.
