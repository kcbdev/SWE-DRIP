# PBI-032: Author working agent stack — compose (postgres + paperclip-from-source + hermes gateway) + hermes config + lint gate

## Directive

Replace the stale `deploy/docker-compose.yml` with the working three-service stack defined in `specs/agent-stack/spec.md`: `postgres` (17-alpine, healthcheck, `pgdata` volume), `paperclip` (**build from `paperclipai/paperclip` source**, port 3100, `paperclip-data:/paperclip`, env placeholders `BETTER_AUTH_SECRET`/`PAPERCLIP_TOOL_ACTION_SIGNING_SECRET`/`PAPERCLIP_PUBLIC_URL=https://paperclip.kcb.ma`/`DATABASE_URL=postgres://paperclip:…@postgres:5432/paperclip`), and `hermes` (`nousresearch/hermes-agent:latest`, `command: gateway run`, `API_SERVER_ENABLED=true`, `API_SERVER_KEY` env placeholder, **volume `hermes-data:/opt/data`**, expose 8642 internal, `HERMES_DASHBOARD=0`, mem 4G/cpus 2/shm 1g). Add `deploy/stack/hermes-config.yaml` seed (`provider: openrouter`, `max_tokens: 4096`, per-agent models per `docs/03-agents.md`). Extend `scripts/lint.js` with an agent-stack gate (no `sk-or-`/hex secrets in compose/config; hermes mounts `/opt/data`; paperclip has `build:` not `image: paperclipai/paperclip`). Add `tests/agent-stack.test.js` offline checks. Update `deploy/README.md` to the new stack. No secrets baked anywhere.

## Context

- Spec: `specs/agent-stack/spec.md` (contracts 1,2,3)
- Depends on: none
- Files/modules touched: `deploy/docker-compose.yml` (replace), `deploy/stack/hermes-config.yaml` (new), `deploy/README.md` (update), `docs/agent-stack-provision.md` (new — runbook consumed by PBI-033), `scripts/lint.js` (extend), `tests/agent-stack.test.js` (new)
- Plane: `kcb/SWDR-32` (Todo — 9a4735a2-d9cf-42fc-aba4-a02724c8c318)

## Verification

- Links to agent-stack contracts 1–3.
- `npm run verify` green (build + lint incl. new agent-stack gate + tests 46 + agent-stack).
- Grep gate: no `sk-or-` and no `a16ac6f`/64-hex in `deploy/` tracked files.
- `docker compose -f deploy/docker-compose.yml config` parses (if docker present); else lint structural checks stand in.

## Refinement rule

- If the official Hermes image layout differs from `/opt/data` (verified upstream docs), stop and update the spec before continuing — memory path is a hard contract.
