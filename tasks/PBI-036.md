# PBI-036: Fourthwall product-creation fix — image upload 403 → working create path

## Directive

Fix the blocked Fourthwall product-creation path: the GCS presigned-upload PUT returns 403, so `POST /open-api/v1.0/products` (which requires `regions:[{region, imageId}]` for type:design) cannot complete and the design pipeline errors with "width/height Too small". Reverse the correct upload→register→create flow from the Fourthwall OpenAPI and implement a working client (`scripts/fw-product-create.js`) that: uploads the design image bytes to the presigned URL (or the correct documented alternative), registers an imageId, and creates a **draft (unpublished)** product in Terminal Collection at $32. Update `specs/fourthwall-store/spec.md` with the corrected schema/flow. Do not publish a product — draft only, founder reviews the design.

## Context

- Spec: `specs/fourthwall-store/spec.md` (contract 4) — update
- Depends on: none (independent of the agent stack)
- Files/modules touched: `scripts/fw-product-create.js` (new), `specs/fourthwall-store/spec.md` (update), `workspace/` (draft product record)
- Plane: `kcb/SWDR-36` (Todo — 8ec8f21a-ef3e-4230-80f0-2d79cd4aabc8)

## Verification

- Links to fourthwall-store contract 4 (updated).
- `node scripts/fw-product-create.js --draft` exits 0 and prints the created product id + URL; Fourthwall dashboard shows 1 unpublished draft in Terminal Collection with correct image.
- Upload step no longer returns 403 (or uses a documented equivalent path).

## Refinement rule

- **Manual** PBI (live Fourthwall API with real credentials). If the 403 persists after following the documented upload flow, capture the exact request/response pair, record it in the spec as a known blocker, and flag to founder — do not force a product through an undocumented path.
