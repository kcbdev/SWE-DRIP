# PBI-022: Rubric calibration suite — known examples, regen evidence, live script (A10/A12)

## Directive

Deliver the A10/A12 evidence: add `pipeline/tests/fixtures/qc/` (the known negative "ninja" and positive "rocket" images, or pinned representations), `pipeline/tests/test_rubric_calibration.py` asserting the rubric scores the negative below 70 and the positive above it against pinned model responses, and `scripts/qc_calibration.py` — a live runner (explicit `OPENROUTER_API_KEY` flag) that performs the same assertion against real vision calls and prints structured evidence. Add the `rubric_version` constant to `pipeline/rubric.py` (minimal edit; scoring logic changes only if the pinned responses reveal a genuine rubric defect — document the reasoning). Strengthen the regeneration test to prove the next render prompt materially differs and includes the rejection note (A12) if PBI-013 coverage is thin.

Scope boundaries: no UI; no threshold changes (70 stays); no tuning against the specific fixture images — the rubric prompt may only change for generalizable reasons, recorded with the PBI resolution.

## Context

- Spec: `specs/design-qc/spec.md`
- Depends on: PBI-013 (rubric + regen loop)
- Files/modules touched: `pipeline/tests/fixtures/qc/*`, `pipeline/tests/test_rubric_calibration.py`, `scripts/qc_calibration.py`, `pipeline/rubric.py` (version constant; minimal)
- Plane: kcb/SWDRP-22

## Verification

- Contracts C1, C3 of the spec.
- Executable: `python -m pytest pipeline/tests -q` green (calibration assertions with fixtures; regen prompt diff + note inclusion); `cmd /c "npm run verify"` green.
- Manual: `python scripts/qc_calibration.py --live` with a real key returns ninja < 70 < rocket and prints both score sets.

## Refinement rule

- If the live run disagrees with the fixture run, the fixtures are stale — refresh pins and record the drift; never loosen the assertion to pass.
- Any rubric prompt change must be justified as generalizable (e.g. explicitly instructing mixed-style detection) and noted in the resolution.
