# PBI-014: Analytics Agent — weekly_report + kill/scale + program.md update

## Directive
Implement the Analytics Agent (`agents/analytics/`) that makes the company smarter every Friday (`cron Fri 18:00 Marrakech`, `claude-haiku-4-5`, budget $3, output `/workspace/weekly_report.md` + `/workspace/kill_list.md`). Pull FW MCP analytics suite (`ecommerce_get-analytics`, `ecommerce_list-offers`, `ecommerce_get-payout-transactions` where needed) via fixtures, produce markdown with exact headers `## KPIs | Metric This Week Last Week Target Status | Orders Revenue Etsy conv rate 2% AOV $35 Top design | ## Kill list (0 sales >30d) | ## Scale list (5+ sales <14d) | ## Anomalies | ## CEO recommendations (3 bullets)`; auto-archive kill list `0 sales && daysSincePublish>30` via `ecommerce_update-offer status:inactive` and log `kill:"{design}" 0 sales 31d` to `program.md` APPEND (via platform PBI-006 helper), notify CEO via `workspace/reports/weekly_report.md` summary task. Do not mutate Email/Finance — only analytics artifacts.

## Context
- Spec: `specs/operations/spec.md` (contract 1)
- Depends on: PBI-010 (needs offers + sales history) and PBI-006 (append helper + program.md path)
- Files/modules touched:
  - `agents/analytics/SOUL.md` (new — haiku, Fri 18:00, tools FW analytics suite + file_write/memory, auto-actions without CEO)
  - `agents/analytics/agent.js` (new — `buildWeeklyReport(analyticsFixture)`, `killDecision(offer)`, `scaleDecision(offer)`, `appendKillPattern()`)
  - `workspace/weekly_report.md` + `workspace/kill_list.md` (new — outputs)
  - `workspace/fixtures/analytics-sample.json` + `offers-sample.json` (new)
  - `tests/analytics.test.js` (new — report structure, kill/scale, program.md append, mock FW archive)
- Plane: kcb/SWDR-15 (Todo — 61254583-1ab8-44c0-a3f6-abda24d95290)

## Verification
- Links to spec contract 1
- `npm run verify` green (FW MCP mocked, no live analytics).
- Fixture `offers:[{slug:"exit-0", sales:0, daysSincePublish:31}, {slug:"undefined-behavior", sales:6, daysSincePublish:10}]` → `killDecision` true only for exit-0 (calls `ecommerce_update-offer {status:inactive}` capture), `scaleDecision` true only for undefined-behavior; `buildWeeklyReport` includes KPI table with `Target 2%` + `Status` column and 3 CEO bullets snapshot-asserted.
- `program.md` append after report contains `kill: exit-0 0 sales 31d`.
- Review comment includes report excerpt + kill/scale JSON.

## Refinement rule
- If FW analytics shape adds `conversionRate` as `0.025` not `2.5%`, normalize in `buildWeeklyReport` but keep `Target 2%` display — do not weaken kill/scale thresholds.
