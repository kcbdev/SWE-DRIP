# PBI-013: Nodes 6-8 — placement/colorway resolver, aesthetic QC + regen, technical QC

## Directive

Implement nodes 6–8: `pipeline/nodes/placement.py` (pure deterministic function: collection contract + design type → placement zones + contrast-valid colorways; no model calls), `pipeline/rubric.py` (the four-criteria rubric definition — style-cohesion, focal-point, placement-fit, contrast — threshold 70, prompt/schema for vision scoring), `pipeline/nodes/aesthetic_qc.py` (vision model scores the render; pass continues; fail regenerates with rubric feedback in the prompt, capped retries; exhausted retries fall to a human-review `interrupt()`), `pipeline/nodes/technical_qc.py` (RGBA/dimensions/transparency validation). Extend `pipeline/graph.py` with the conditional edges. Tests: placement determinism table; contrast filtering by colorway; rubric threshold boundary; regeneration receives the rejection feedback and is capped; technical QC rejects wrong mode/dimensions/opaque alpha.

Scope boundaries: no calibration against the known ninja/rocket examples (design-qc spec owns that validation suite); no QC UI.

## Context

- Spec: `specs/pipeline-core/spec.md`
- Depends on: PBI-012
- Files/modules touched: `pipeline/nodes/placement.py`, `pipeline/nodes/aesthetic_qc.py`, `pipeline/nodes/technical_qc.py`, `pipeline/rubric.py`, `pipeline/graph.py` (extend), `pipeline/tests/test_nodes_placement_qc.py`
- Plane: (pending sync)

## Verification

- Contracts C8, C9, C2 (nodes 6–8) of the spec.
- Executable: `python -m pytest pipeline/tests -q` green (deterministic placement table; threshold 69/70/71; regen cap + feedback assertion; RGBA/dimension rejections); `cmd /c "npm run verify"` green.
- Manual: render a fixture through nodes 6–8 with a mocked vision response → scores + placement written to state.

## Refinement rule

- If the rubric needs per-criterion prompts/schemas beyond a single vision call, split into multiple calls only if the routing table covers them — otherwise amend the spec first.
- The regeneration cap is a constant in `pipeline/rubric.py`; changing it is a spec change.
