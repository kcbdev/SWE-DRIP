# PBI-020: Materialize 11 SOUL.md files — agent identities for Paperclip hiring

## Directive
Create `agents/souls/{ceo,trend-scout,copy,design,listing,social,video,analytics,email,community,finance}.md` — one paste-ready identity per agent per the `docs/03-agents.md` templates: role/identity paragraph, model line (must equal registry value from `agents/lib/config.js`), trigger (cron/on_task), budget cap, tools list, output contract, decision rules. CEO file may adapt `agents/ceo/SKILL.md`. Extend `scripts/lint.js` with a souls gate: every file exists and contains its registry model string + budget cap + a trigger keyword; lint fails otherwise. Do not invent new roles or change budgets/models.

## Context
- Spec: `specs/paperclip-org/spec.md` (contract 5)
- Depends on: none (pure repo work)
- Files/modules touched: `agents/souls/*.md` (11 new), `scripts/lint.js` (edit)
- Plane: `kcb/SWDR-21` (Todo — 2b465f21-86e3-4e16-b51a-f75a172ed452)

## Verification
- Links to spec contract 5
- `npm run verify` green with new souls lint checks
- Each souls file contains: its model string (e.g. `google/gemini-2.0-flash-001` for trend-scout), `$N/mo` budget matching config.js, trigger keyword (`cron Mon 08:00` / `on_task`)
- Trend Scout soul includes scoring rubric 0–40/0–30/0–30 + ≥60 threshold + Etsy-dupe rule; Design soul includes FLUX prompt template + fallback model; Social soul includes 4:1 ratio + no-hashtags + Reddit-no-body-link rules

## Refinement rule
- If docs/03-agents.md and agents/lib/config.js ever disagree, stop and flag — do not pick one silently.
