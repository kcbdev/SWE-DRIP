# PBI-003: Bind domain `swedrip.kcb.ma` with Traefik + Let's Encrypt auto-TLS

## Directive
Attach domain `https://swedrip.kcb.ma` to the Coolify app `swedrip` on server `kcb` (PBI-002's app), enable Traefik with Let's Encrypt auto-issue/renew, enforce `http→https` 301, and confirm HSTS where Traefik emits it. Add or verify the DNS record (`swedrip` → `kcb` IP) as a pre-step and document it. Do not change the app's Dockerfile, port, or healthcheck — only the Coolify Domains / Traefik config.

## Context
- Spec: `specs/coolify-deploy/spec.md` (contracts 3, 6)
- Depends on: PBI-002 (app must exist before a domain can be bound)
- Files/modules touched:
  - `docs/adrs/ADR-002.md` (edit — append domain/TLS section: domain value `https://swedrip.kcb.ma`, Traefik Let's Encrypt issuer, http→https redirect, cert auto-renew)
  - `.coolify/app.json` or `docs/coolify-provision.md` (edit — add `domains: ["https://swedrip.kcb.ma"]`, note `forceHttps: true`)
  - `docs/04-deploy.md` or `README.md` (edit — 1-paragraph delta noting `swedrip.kcb.ma` binding + Coolify Domains path; do not restructure the 7-day guide)
  - `scripts/verify-domain.js` or `tests/domain.test.js` (optional helper — https probe; not the gate, but evidence)
- Plane: `kcb/SWDR-3` (Todo — 2d3e2cfd-5691-49a1-b4e8-c4cc02f41714)
- External dependency: DNS `kcb.ma` zone (founder/admin must add `swedrip` A/CNAME → `kcb` IP); if missing, PBI stays `In Progress` with explicit `How to review / reproduce` DNS step

## Verification
- Links to spec contract 3
- `npm run verify` still green.
- Live checks (record outputs in review comment):
  - `curl -s -o /dev/null -w "%{http_code}" http://swedrip.kcb.ma/` → `301` and `Location: https://swedrip.kcb.ma/`
  - `curl -s https://swedrip.kcb.ma/ | grep -q "SWE Drip"` → 0 (200, correct content, not Traefik 404)
  - `curl -s https://swedrip.kcb.ma/health | grep -q '"status":"ok"'` → 0 and `-w "%{http_code}"` → `200`
  - `echo | openssl s_client -connect swedrip.kcb.ma:443 -servername swedrip.kcb.ma 2>/dev/null | openssl x509 -noout -issuer | grep -qi "let's encrypt"` → issuer is Let's Encrypt (not self-signed); `notAfter` is > 30 days out
  - `curl -Ik https://swedrip.kcb.ma/ | tr A-Z a-z | grep -q "strict-transport-security"` → if Traefik HSTS is on, note value; if off, record explicitly and file a follow-up — do not fail the PBI for HSTS alone if Coolify default omits it
- No secret leakage via headers; `Server` header may be Traefik/nginx — tolerated.
- Redeploy still succeeds within 10 min of domain change without manual SSH (gate 6 partial — full runbook is PBI-004).

## Refinement rule
- If DNS cannot be set by the agent (zone outside `kcb`), keep the PBI in `In Progress` and add `manual` review with exact `nslookup swedrip.kcb.ma` vs `kcb` IP comparison — do not silently switch to a different domain.
