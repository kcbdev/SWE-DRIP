# PBI-037: First product E2E — brief to live Fourthwall listing via the working agent stack

## Directive

Run the complete pipeline once through the working Paperclip + Hermes stack: CEO assigns an approved brief → Trend Scout scores → Copy Agent slogan (≤6 words) + listing copy → Design Agent generates the design (FLUX) and calls FW `ecommerce_generate-product-design-previews` → `ecommerce_create-offers-from-designs` → Listing Agent sets price $32 / tags / Terminal Collection via `ecommerce_update-offer` and confirms live. Capture every task's output on the board as agent-attributed evidence (no human did the work). This is the proof the autonomous company functions per `docs/03-agents.md` + `docs/06-traffic.md` flywheel.

## Context

- Spec: `specs/agent-stack/spec.md` (contract 5,6) + `specs/fourthwall-store/spec.md` (contract 4) + `specs/paperclip-org/spec.md` (contract 4)
- Depends on: PBI-034, PBI-036
- Files/modules touched: tracking only; `workspace/listing_copy.json` may receive the `product_url` record for repo-side memory
- Plane: `kcb/SWDR-37` (Todo — 95e7b764-b3e1-484c-b1ca-dd0031c852c4)

## Verification

- Links to agent-stack contracts 5–6 and fourthwall-store contract 4.
- Live product visible at `swedrip.fourthwall.com` — t-shirt, price exactly $32, in Terminal Collection, brand-correct preview image (#0D0D0D/#00FF41).
- Task chain evidence: 4+ tasks on the board, each with agent-attributed outputs.
- Founder visual approval of the first design before it stays published.

## Refinement rule

- If any agent step fails (e.g. FLUX text rendering), follow that agent's SOUL fallback rules; if still failing after fallback, keep the product unpublished and flag — never ship a broken first impression.
